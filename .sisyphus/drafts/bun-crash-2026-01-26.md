# Draft: Bun Crash Investigation (2026-01-26)

## Requirements (confirmed)
- User ran bun and the app crashed; want error cause analysis from screenshot.
- Do not use bun or LSP going forward.
- User asked to identify what they were working on; pointed to `.sisyphus/plans/shortslab-two-step-alignment.md`.

## Technical Decisions
- No bun/LSP usage in any future steps (per user request and repo rules).

## Research Findings
- The referenced plan focuses on aligning two-step generation with AI script generation: enforce fixed phrases/character descriptors, normalize character IDs, and enforce diversity (expression/camera angle).
- The plan includes 5 TODOs; likely active work relates to Task 2–4 (prompt composition alignment, character normalization, diversity rules).
- The plan does NOT mention bun; it uses `npm run dev` for manual verification. Any bun usage likely came from habit (e.g., `bun install` or `bun run dev`) rather than from the plan's steps.

## Open Questions
- Need an ASCII-only path or pasted error text to read screenshot (image tool failing on non-ASCII path).
- Which TODO/task from the plan was being executed when bun crashed?
- What exact bun command was run immediately before the crash?

## Scope Boundaries
- INCLUDE: error message extraction and likely root-cause hypotheses.
- INCLUDE: identify active work from existing plan/checklist.
- EXCLUDE: implementation changes or running commands.
