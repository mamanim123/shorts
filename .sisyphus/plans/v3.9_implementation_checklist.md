# ✅ v3.9 구현 체크리스트

## 1. 프롬프트 구조 최적화 (services/promptBuilder.ts)
- [ ] `mandatoryRules` 내의 `START` 및 `END` 상수 배치를 'Global Quality Footer' 방식으로 변경 가능한지 검토.
- [ ] 투샷/쓰리샷 생성 시 `[Person 1: ...]` 형식 내의 중복 형용사 제거 및 핵심 속성 위주 재배치.
- [ ] `NO SUMMARIZATION` 규칙이 모든 `longPrompt` 생성 로직에 누락 없이 포함되어 있는지 재검증.

## 2. 겨울 테마 고도화 (services/labPromptBuilder.ts)
- [ ] `WINTER_COLLECTION` 내 악세서리 묘사 문구에 마마님 취향의 럭셔리 키워드(Mink, Velvet, Crystal 등) 보강.
- [ ] `convertToTightLongSleeveWithShoulderLine` 함수에서 'Halter-neck'이나 'Off-shoulder' 변환 시 디자인 연속성(Color 유지) 로직 점검.
- [ ] `applyWinterLookToExistingPrompt` 실행 시 배경(`snow`)과 악세서리가 `technicalPart`를 침범하지 않도록 정규식(Regex) 정밀 튜닝.

## 3. 주인공 성별 및 장르 로직 (services/labPromptBuilder.ts & genreService.ts)
- [ ] `buildLabScriptPrompt` 호출 시 `gender` 값에 따른 나레이터(`narrator.slot`) 스위칭 로직(WomanA ↔ ManA) 확정.
- [ ] `MAMA_CHARACTER_PRESETS.MALE`의 묘사를 마마님 스타일(댄디, 세련, 운동체형)로 업데이트.
- [ ] 남성 주인공일 때도 `enforceKoreanIdentity`가 "Handsome Korean man"으로 정확히 작동하는지 확인.

## 4. 일관성 유지 및 검증 시스템
- [ ] `lockedOutfits` 객체에 겨울 악세서리가 포함된 '최종 의상 풀네임'이 정확히 저장되는지 확인.
- [ ] 8~12개 가변 씬 시스템에서 1:1 매칭이 깨지지 않도록 `scriptBody` 파싱 로직 안정화.
- [ ] 모든 `longPrompt` 맨 앞에 카메라 앵글과 표정 키워드가 최우선으로 배치되도록 순서 조정.

## 5. 최종 테스트 및 안정화
- [ ] '겨울 악세서리 활성화' + '남성 주인공' 조합으로 테스트 대본 생성 및 프롬프트 일관성 검수.
- [ ] '겨울 악세서리 비활성화' + '여성 주인공' 조합으로 기존 퀄리티 유지 여부 확인.
- [ ] LSP 및 Bun 사용 없이 `npm run dev` 환경에서 정상 동작 확인.
