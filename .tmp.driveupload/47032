#!/bin/bash
PROMPT_FILE="$(pwd)/.codex/prompts/algorithmic-art.txt"
if [ ! -f "$PROMPT_FILE" ]; then
  echo "프롬프트 파일을 찾을 수 없습니다: $PROMPT_FILE" >&2
  exit 1
fi
if [ $# -eq 0 ]; then
  echo "사용법: codex-algorithmic-art <설명 또는 파일>" >&2
  exit 1
fi
codex run --prompt-file "$PROMPT_FILE" "$@"
