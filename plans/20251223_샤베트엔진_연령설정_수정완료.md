# 샤베트 엔진 연령 설정 수정 완료

**작업일**: 2025-12-23  
**상태**: ✅ 완료

---

## 수정 내용

### 1. `engine_config.json` 수정

4개의 샤베트 엔진 프롬프트에서 하드코딩된 연령 텍스트를 플레이스홀더로 교체:

**수정된 엔진**:
- `CUSTOM_1765968277784` (샤베트백업본)
- `CUSTOM_1765973385899` (샤베트)
- `CUSTOM_1766109481051` (샤베트의상,헤어)
- `CUSTOM_1766400278267` (1234)

**변경 사항**:
```
변경 전: "시스템에서 설정된 나이"
변경 후: "{{TARGET_AGE}}"

변경 전: "[시스템에서 설정된 나이]"
변경 후: "{{TARGET_AGE_EN}}"
```

### 2. `services/geminiService.ts` 수정

**위치**: Line 1107-1116

**추가된 로직**:
```typescript
} else if (isCustomEngine) {
  // [NEW] Generate age tokens for custom engines (e.g., Sherbet engines)
  const customAgeTokens = buildAgeTokens(input.targetAge);
  
  // [NEW] Replace placeholders in custom engine prompt with actual age values
  systemPrompt = selectedEnginePrompt
    .replace(/\{\{TARGET_AGE\}\}/g, customAgeTokens.korean)
    .replace(/\{\{TARGET_AGE_EN\}\}/g, customAgeTokens.englishShort);
  
  console.log(`[Engine] Custom engine "${input.engineVersion}" selected with age: ${customAgeTokens.korean}`);
}
```

**기능**:
- 사용자가 선택한 연령 (예: "50대")을 `buildAgeTokens()` 함수로 변환
- `{{TARGET_AGE}}` → "50대" (한국어)
- `{{TARGET_AGE_EN}}` → "in her 50s" (영어)
- 변환된 값으로 커스텀 엔진 프롬프트의 플레이스홀더를 치환

---

## 테스트 방법

### 테스트 케이스 1: 샤베트 엔진 + 50대

1. 서버 재시작:
   ```bash
   npm run dev
   ```

2. 쇼츠 생성기에서:
   - 엔진: "샤베트의상,헤어" 선택
   - 연령: "50대" 선택
   - 주제: "골프장에서의 오해" 입력
   - "대본 생성" 클릭

3. **기대 결과**:
   - ✅ 대본에 "50대" 언급
   - ✅ 샤베트 톤 (상큼·발랄·엉뚱·귀여운)
   - ✅ 반전 코미디 구조
   - ✅ 슬롯 기반 헤어 스타일 적용
   - ✅ 콘솔에 `[Engine] Custom engine "CUSTOM_1766109481051" selected with age: 50대` 출력

### 테스트 케이스 2: 샤베트 엔진 + 40대

1. 동일 설정, 연령만 "40대"로 변경

2. **기대 결과**:
   - ✅ 대본에 "40대" 언급
   - ✅ 콘솔에 `selected with age: 40대` 출력

### 테스트 케이스 3: V3 엔진 정상 작동 확인

1. 럭셔리 엔진 V3 선택
2. 기존 기능 정상 작동 확인

---

## 백업 파일

수정 전 원본 파일은 다음 위치에 백업되어 있습니다:
- `engine_config.json.backup_20251223_HHMMSS`
- `geminiService.ts.backup_20251223_HHMMSS`

롤백이 필요한 경우:
```bash
Copy-Item "engine_config.json.backup_*" "engine_config.json"
Copy-Item "services\geminiService.ts.backup_*" "services\geminiService.ts"
```

---

## 영향 범위

### 수정된 파일
- ✅ `engine_config.json` (4개 샤베트 엔진 프롬프트)
- ✅ `services/geminiService.ts` (커스텀 엔진 변수 치환 로직)

### 영향받는 기능
- ✅ 샤베트 엔진: 연령 반영 정상화
- ✅ 커스텀 엔진: 동적 변수 지원 추가
- ✅ V3 엔진: 영향 없음 (별도 경로)
- ✅ 다른 커스텀 엔진: 플레이스홀더 미사용 시 영향 없음

---

## 다음 단계

1. **서버 재시작** 필수
2. **테스트 수행** (위 테스트 케이스 1, 2, 3)
3. **결과 확인** 후 문제 없으면 완료
4. 문제 발생 시 백업 파일로 롤백 후 재분석
