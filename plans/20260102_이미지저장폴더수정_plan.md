# Implementation Plan: 이미지 저장 폴더 수정

**작성일**: 2026-01-02  
**작업명**: 이미지 저장 폴더 수정  
**버전**: v5.5 → v5.6

---

## 📋 문제 분석

### 현재 상황
1. ✅ **대본 생성 시**: `골프장의_위험한_재회-2` 폴더가 정상적으로 생성됨
2. ❌ **이미지 생성 시**: `storyId`가 `unknown`, `title`이 `N/A`로 전달되어 `orphaned` 폴더에 저장됨

### 로그 분석
```
[ImageAI] Forwarding scene 1 from story unknown (title: N/A) to GEMINI
📁 Story folder ready: orphaned (images: generated_scripts\images\orphaned)
```

### 근본 원인
`OutputDisplay.tsx`의 `handleForwardPromptToImageAI` 함수에서:
- `ensureStoryFolderReady()`를 호출하여 `folderName`을 얻음
- 하지만 **대본 생성 직후 `storyFolderName` 상태가 업데이트되지 않음**
- 결과적으로 `folderName`이 빈 값이거나 `undefined`로 전달됨

---

## 🎯 해결 방안

### 1단계: 대본 생성 시 폴더 정보 전달 확인
대본 생성 API 응답에서 `folderName` 정보가 제대로 전달되는지 확인

**파일**: `server/index.js`
- `/api/generate` 엔드포인트 응답에 `folderName` 포함 여부 확인

### 2단계: OutputDisplay 컴포넌트에서 폴더 정보 수신
대본 데이터를 받을 때 `_folderName` 속성이 제대로 설정되는지 확인

**파일**: `components/OutputDisplay.tsx`
- `data._folderName` 속성 확인
- `ensureStoryFolderReady()` 함수의 로직 검증

### 3단계: 이미지 생성 시 폴더 정보 전달 보장
`handleForwardPromptToImageAI` 함수에서 `storyId`와 `title`이 제대로 전달되도록 수정

**파일**: `components/OutputDisplay.tsx`
- Line 629: `ensureStoryFolderReady()` 호출 결과 확인
- Line 630: `normalizedTitle` 값 확인
- Line 642-646: API 요청 body에 올바른 값 전달 확인

---

## 🔧 수정 계획

### 수정 1: 디버깅 로그 추가
`OutputDisplay.tsx`의 `handleForwardPromptToImageAI` 함수에 디버깅 로그 추가

```typescript
const handleForwardPromptToImageAI = async (prompt: string, id: string, sceneNumber?: number) => {
  // ... 기존 코드 ...
  
  try {
    console.log('[DEBUG] Before ensureStoryFolderReady:', {
      storyFolderName,
      selectedTitle,
      dataTitle: data.title,
      dataId: data.id,
      data_folderName: (data as any)?._folderName
    });
    
    const folderName = await ensureStoryFolderReady();
    
    console.log('[DEBUG] After ensureStoryFolderReady:', {
      folderName,
      storyFolderName
    });
    
    const normalizedTitle = (selectedTitle || data.title || '').trim() || data.title || 'Untitled';
    
    console.log('[DEBUG] Request payload:', {
      storyId: folderName,
      title: normalizedTitle,
      sceneNumber
    });
    
    // ... 나머지 코드 ...
  }
}
```

### 수정 2: ensureStoryFolderReady 함수 개선
폴더 생성 후 상태 업데이트가 제대로 되도록 보장

```typescript
const ensureStoryFolderReady = useCallback(async (): Promise<string> => {
  // 1. 이미 storyFolderName이 있으면 사용
  if (storyFolderName) {
    console.log(`[ensureStoryFolderReady] Using existing folder: ${storyFolderName}`);
    return storyFolderName;
  }

  // 2. data._folderName 확인
  const existingFolderName = (data as any)?._folderName;
  if (existingFolderName && typeof existingFolderName === 'string') {
    console.log(`[ensureStoryFolderReady] Using data._folderName: ${existingFolderName}`);
    updateStoryFolderBinding(existingFolderName);
    return existingFolderName;
  }

  // 3. 새 폴더 생성
  const normalizedTitle = (selectedTitle || data.title || '').trim() || `Story_${data.id}`;
  console.log(`[ensureStoryFolderReady] Creating new folder for: ${normalizedTitle}`);

  try {
    const response = await fetch('http://localhost:3002/api/create-story-folder', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ title: normalizedTitle })
    });

    if (!response.ok) {
      console.error(`[ensureStoryFolderReady] HTTP error: ${response.status}`);
      throw new Error('스토리 폴더를 준비하지 못했습니다.');
    }

    const payload = await response.json();
    if (!payload?.success || !payload?.folderName) {
      console.error('[ensureStoryFolderReady] Invalid response:', payload);
      throw new Error(payload?.error || '스토리 폴더 생성에 실패했습니다.');
    }

    console.log(`[ensureStoryFolderReady] Folder created: ${payload.folderName}`);
    updateStoryFolderBinding(payload.folderName);
    
    // ✅ 추가: 상태 업데이트 후 반환 전에 약간의 지연
    await new Promise(resolve => setTimeout(resolve, 100));
    
    return payload.folderName;
  } catch (error) {
    console.error('[ensureStoryFolderReady] Error:', error);
    throw error;
  }
}, [storyFolderName, selectedTitle, data.id, data.title, data, updateStoryFolderBinding]);
```

### 수정 3: 대본 생성 후 폴더 정보 즉시 업데이트
대본 생성 API 응답에서 `_folderName`을 받아 즉시 상태 업데이트

**확인 필요**: `youtube-shorts-script-generator.tsx`에서 대본 생성 후 `OutputDisplay`에 데이터를 전달할 때 `_folderName` 포함 여부

---

## ✅ 테스트 계획

1. **대본 생성 테스트**
   - 새 대본 생성
   - 콘솔에서 폴더 생성 로그 확인
   - `generated_scripts/images/` 폴더에 올바른 이름의 폴더 생성 확인

2. **이미지 생성 테스트**
   - 생성된 대본에서 "AI생성" 버튼 클릭
   - 콘솔에서 디버깅 로그 확인:
     - `storyId`가 올바른 폴더명으로 전달되는지
     - `title`이 올바르게 전달되는지
   - 이미지가 올바른 폴더에 저장되는지 확인

3. **엣지 케이스 테스트**
   - 대본 생성 직후 즉시 이미지 생성
   - 여러 대본을 생성한 후 각각 이미지 생성
   - 기존 대본에서 이미지 생성

---

## 📝 작업 순서

1. ✅ 문제 분석 완료
2. ⏳ 디버깅 로그 추가 (마마님 승인 대기)
3. ⏳ 실제 동작 확인 및 원인 파악
4. ⏳ 수정 사항 적용
5. ⏳ 테스트 및 검증
6. ⏳ 버전 업데이트 (v5.5 → v5.6)

---

## 🚨 주의사항

- 코드 수정 전 **반드시 마마님의 승인** 필요
- 수정 후 기존 기능에 영향이 없는지 확인
- 모든 변경사항은 로그로 추적 가능하도록 작성
