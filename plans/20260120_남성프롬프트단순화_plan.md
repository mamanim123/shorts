# Simplifying Male Character Prompts

The user requested to simplify male character prompts to just "Korean man", as detailed aesthetic descriptions are not necessary for male characters.

## Proposed Changes

### 1. Prompt Builder Service

#### [MODIFY] [labPromptBuilder.ts](file:///f:/test/쇼츠대본생성기-v3.5.1/services/labPromptBuilder.ts)
- `MAMA_CHARACTER_PRESETS.MALE`의 `identity`를 "Korean man"으로 단순화하고, `bodyType`, `style`, `outfitFit`을 빈 문자열 또는 최소화된 값으로 변경합니다.
- `PROMPT_CONSTANTS.MALE_BODY`를 "Korean man"으로 단순화합니다.

### 2. UI Panel Component

#### [MODIFY] [ShortsLabPanel.tsx](file:///f:/test/쇼츠대본생성기-v3.5.1/components/ShortsLabPanel.tsx)
- `enforceKoreanIdentity` 함수에서 남성일 경우 `identityDescriptor`를 "Korean man"으로 단순화합니다.
- `generatePrompt` 함수에서 남성일 경우 `identity`를 "Korean man"으로 단순화합니다.

## Checklist

- [ ] `labPromptBuilder.ts` 수정 (남성 프롬프트 단순화)
- [ ] `ShortsLabPanel.tsx` 수정 (남성 프롬프트 단순화)
- [ ] 수동 검증 및 결과 확인
- [ ] 최종 커밋 및 보고
