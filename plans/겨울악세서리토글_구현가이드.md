# 겨울 악세서리 토글 기능 구현 가이드

## 1. State 추가 (ShortsLabPanel.tsx, 라인 306 근처)

```typescript
const [currentFolderName, setCurrentFolderName] = useState<string | null>(null);

// [NEW] 겨울 악세서리 토글 상태
const [useWinterAccessories, setUseWinterAccessories] = useState(false);

// 이미지 생성 상태 (신규!)
```

## 2. UI 버튼 추가 (설정 탭 영역, 라인 1900 근처)

```typescript
{/* 겨울 악세서리 토글 */}
<SettingSection title="🧣 겨울 악세서리" description="여성 캐릭터에 겨울 아우터와 악세서리 자동 추가">
    <button
        onClick={() => setUseWinterAccessories(!useWinterAccessories)}
        className={`px-4 py-2 rounded-lg font-medium transition-all ${
            useWinterAccessories
                ? 'bg-blue-600 text-white'
                : 'bg-slate-800 text-slate-300 hover:bg-slate-700'
        }`}
    >
        {useWinterAccessories ? '✅ 활성화' : '❌ 비활성화'}
    </button>
    {useWinterAccessories && (
        <p className="text-xs text-blue-400 mt-2">
            패딩, 귀도리, 비니 등이 자동으로 추가됩니다
        </p>
    )}
</SettingSection>
```

## 3. labPromptBuilder.ts 수정

`buildLabScriptPrompt` 함수에 `useWinterAccessories` 파라미터 추가:

```typescript
export const buildLabScriptPrompt = (
  topic: string,
  genre: string,
  targetAge: string,
  gender: 'female' | 'male',
  additionalContext?: string,
  useWinterAccessories?: boolean  // [NEW]
): string => {
  // ... 기존 코드 ...
  
  // 겨울 테마 감지 로직 수정
  const shouldApplyWinter = useWinterAccessories !== undefined 
    ? useWinterAccessories  // 명시적 플래그가 있으면 우선
    : isWinterTopic(topic);  // 없으면 자동 감지
    
  if (shouldApplyWinter) {
    const { outerwear, accessories } = selectWinterItems();
    womanAOutfit = applyWinterItems(womanAOutfit, outerwear, accessories);
    womanBOutfit = applyWinterItems(womanBOutfit, outerwear, accessories);
    womanDOutfit = applyWinterItems(womanDOutfit, outerwear, accessories);
  }
  
  // ... 나머지 코드 ...
};
```

## 4. 호출부 수정

대본 생성 API 호출 시 `useWinterAccessories` 전달:

```typescript
const response = await fetch('http://localhost:3002/api/lab/generate-script', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    topic: aiTopic,
    genre: aiGenre,
    targetAge: aiTargetAge,
    gender: settings.koreanGender,
    useWinterAccessories: useWinterAccessories  // [NEW]
  })
});
```

## 5. 서버 측 수정 (server/index.js)

```javascript
app.post('/api/lab/generate-script', async (req, res) => {
  const { topic, genre, targetAge, gender, useWinterAccessories } = req.body;
  
  const prompt = buildLabScriptPrompt(
    topic,
    genre,
    targetAge,
    gender,
    undefined,
    useWinterAccessories  // [NEW]
  );
  
  // ... 나머지 코드 ...
});
```

이 가이드대로 수정하시면 됩니다!
