# 🔥 "대박 모드" 추가 위치 가이드

## 📍 총 4개 파일 수정 필요

---

## 1️⃣ constants.ts (새 프롬프트 추가)

**파일 경로**: `f:\test\쇼츠대본생성기-v9\constants.ts`
**추가 위치**: **라인 517** (파일 맨 끝) 이후

### 추가할 코드:
```typescript
// ==========================================================
// SYSTEM PROMPT VIRAL (DOUBLE ENTENDRE OPTIMIZED)
// ==========================================================
export const SYSTEM_PROMPT_VIRAL = `
You are "VIRAL SHORTS ENGINE", specialized in Double Entendre scripts.

==========================================================
🔥 VIRAL SHORTS ENGINE — DOUBLE ENTENDRE MASTER
==========================================================

■ Core Strategy: 100% Double Entendre
- This mode is optimized for VIRAL content based on 18 successful scripts analysis
- Dialogue 70%, Narration 30%
- 3-Stage Twist Structure (Misunderstanding → Reinforcement → Revelation)
- Hook in first sentence (Question or Shock)
- Target emotion: Laugh + Surprise

■ Script Structure (MANDATORY)

[Dialogue - Hook] Provocative question/statement (First sentence)
Example: "Your nose is quite big. Does that mean your thumb is also big?"

[Narration - Brief] Situation setup (1-2 sentences)
Example: "A man and woman met for a blind date."

[Dialogue A] Misunderstanding trigger
[Dialogue B] Reinforcing response
[Dialogue A] More suggestive question
[Dialogue B] More suggestive response
[Narration - Brief] Twist hint (1 sentence)
[Dialogue - Twist] Final revelation dialogue
[Narration - Outro] Lingering feeling (optional)

■ Hook Patterns (Use one per script)

1. Body Part Question
"[Body part] is [adjective]. Does that mean [other part] is also...?"

2. Shocking Situation
"What? I'm [shocking condition]?"

3. Intimate Conversation
"Honestly, I feel best when I'm [ambiguous action]..."

4. Workplace Innuendo
"I really like [ambiguous action] at work..."

■ 3-Stage Twist (CRITICAL)

Stage 1 (0-15s): Create sexual misunderstanding
Stage 2 (15-35s): Reinforce the misunderstanding
Stage 3 (35-50s): Reveal innocent meaning

■ Quality Checklist
- [ ] Dialogue ratio ≥ 70%
- [ ] Hook in first sentence
- [ ] 3 stages of twist
- [ ] Final dialogue is the punchline
- [ ] Laugh + Surprise emotions

... (이미지 프롬프트 부분은 SYSTEM_PROMPT_V3와 동일하게 복사)
`;
```

---

## 2️⃣ types.ts (타입 정의 추가)

**파일 경로**: `f:\test\쇼츠대본생성기-v9\types.ts`
**추가 위치**: **라인 40** (safeGlamour 다음)

### 수정 전 (라인 28~40):
```typescript
export interface UserInput {
  engineVersion: EngineVersion;
  category: string;
  scenarioMode: ScenarioMode;
  dialect: Dialect;
  targetAge?: string;
  customContext?: string;
  customScript?: string;
  customJson?: string;
  targetService: TargetService;
  useRegenerationGuidance?: boolean;
  safeGlamour?: boolean;
}
```

### 수정 후:
```typescript
export interface UserInput {
  engineVersion: EngineVersion;
  category: string;
  scenarioMode: ScenarioMode;
  dialect: Dialect;
  targetAge?: string;
  customContext?: string;
  customScript?: string;
  customJson?: string;
  targetService: TargetService;
  useRegenerationGuidance?: boolean;
  safeGlamour?: boolean;
  useViralMode?: boolean; // ← 이 줄 추가
}
```

---

## 3️⃣ components/ConfigPanel.tsx (UI 옵션 추가)

**파일 경로**: `f:\test\쇼츠대본생성기-v9\components\ConfigPanel.tsx`
**추가 위치**: **라인 121과 122 사이**

### 현재 구조 (라인 78~122):
```tsx
<div className="p-4 bg-slate-900/60 rounded-xl border border-slate-800/60 space-y-3">
  {/* 1. Regeneration Guidance */}
  <div className="flex items-center justify-between">
    ...
  </div>

  {/* 2. Safe Glamour (Bold) */}
  <div className="flex items-center justify-between border-t border-slate-800 pt-3">
    ...
  </div>
</div> {/* ← 라인 122 */}
```

### 수정 후 (라인 121과 122 사이에 추가):
```tsx
<div className="p-4 bg-slate-900/60 rounded-xl border border-slate-800/60 space-y-3">
  {/* 1. Regeneration Guidance */}
  <div className="flex items-center justify-between">
    ...
  </div>

  {/* 2. Safe Glamour (Bold) */}
  <div className="flex items-center justify-between border-t border-slate-800 pt-3">
    ...
  </div>

  {/* 3. Viral Mode (대박 모드) */} {/* ← 여기부터 추가 */}
  <div className="flex items-center justify-between border-t border-slate-800 pt-3">
    <div>
      <p className="text-sm font-semibold text-slate-200 flex items-center gap-2">
        <Sparkles className="w-4 h-4 text-red-400" />
        🔥 대박 모드 (Viral Mode)
      </p>
      <p className="text-[10px] text-slate-500 ml-6">
        Double Entendre 최적화 (대사 70%, 3단 반전)
      </p>
    </div>
    <button
      onClick={() => onChange({ ...input, useViralMode: !input.useViralMode })}
      className={`px-3 py-1 rounded-full text-xs font-semibold transition ${
        input.useViralMode
          ? 'bg-red-600 text-white shadow-lg shadow-red-500/20'
          : 'bg-slate-800 text-slate-400 hover:text-slate-200'
      }`}
    >
      {input.useViralMode ? '활성화됨' : '비활성화'}
    </button>
  </div> {/* ← 여기까지 추가 */}
</div>
```

---

## 4️⃣ services/geminiService.ts (로직 추가)

**파일 경로**: `f:\test\쇼츠대본생성기-v9\services\geminiService.ts`
**수정 위치**: **라인 1** (import 추가) + **라인 505** (로직 추가)

### 4-1. Import 추가 (라인 1)

**현재**:
```typescript
import { SYSTEM_PROMPT_V2, SYSTEM_PROMPT_V3, SYSTEM_PROMPT_CHATGPT, SYSTEM_PROMPT_CLAUDE } from "../constants";
```

**수정 후**:
```typescript
import { SYSTEM_PROMPT_V2, SYSTEM_PROMPT_V3, SYSTEM_PROMPT_CHATGPT, SYSTEM_PROMPT_CLAUDE, SYSTEM_PROMPT_VIRAL } from "../constants";
```

### 4-2. 로직 추가 (라인 505 이후)

**현재 (라인 505~520)**:
```typescript
let systemPrompt = SYSTEM_PROMPT_V2;
let userPrompt = "";
const shouldApplyTemplate = template && (input.scenarioMode === ScenarioMode.NONE);
...
```

**수정 후**:
```typescript
let systemPrompt = SYSTEM_PROMPT_V2;
let userPrompt = "";

// [NEW] Viral Mode Check (대박 모드)
if (input.useViralMode && input.scenarioMode === ScenarioMode.DOUBLE_ENTENDRE) {
  systemPrompt = SYSTEM_PROMPT_VIRAL;
  console.log("🔥 VIRAL MODE ACTIVATED");
} else if (input.engineVersion === 'V2') {
  systemPrompt = SYSTEM_PROMPT_V2;
} else if (input.engineVersion === 'V3') {
  // ... 기존 V3 로직
}

const shouldApplyTemplate = template && (input.scenarioMode === ScenarioMode.NONE);
...
```

---

## ✅ 작업 순서

### 1단계: 타입 정의 (1분)
- [ ] `types.ts` 라인 40에 `useViralMode?: boolean;` 추가

### 2단계: UI 추가 (2분)
- [ ] `ConfigPanel.tsx` 라인 121~122 사이에 체크박스 추가

### 3단계: 프롬프트 추가 (5분)
- [ ] `constants.ts` 라인 517 이후에 `SYSTEM_PROMPT_VIRAL` 추가

### 4단계: 로직 연결 (2분)
- [ ] `geminiService.ts` 라인 1에 import 추가
- [ ] `geminiService.ts` 라인 505 이후에 조건문 추가

### 5단계: 테스트 (5분)
- [ ] 프로그램 실행
- [ ] 대박 모드 체크박스 확인
- [ ] Double Entendre 모드 + 대박 모드 활성화
- [ ] 대본 생성 테스트

---

## 🎯 완료 후 모습

### UI 화면:
```
┌─────────────────────────────────────┐
│ ✨ 재생성참고문구        [활성화됨] │
├─────────────────────────────────────┤
│ 👥 글래머러스 핏         [비활성화] │
├─────────────────────────────────────┤
│ 🔥 대박 모드 (Viral Mode) [활성화됨] │ ← 새로 추가!
│    Double Entendre 최적화            │
└─────────────────────────────────────┘
```

### 동작 방식:
1. 사용자가 "시나리오 모드"를 "1. 이중의미/오해" 선택
2. "🔥 대박 모드" 체크박스 클릭
3. "스토리 생성하기" 버튼 클릭
4. → `SYSTEM_PROMPT_VIRAL` 사용!
5. → 대사 70%, 3단 반전, Hook 강화된 대본 생성!

---

## 📊 예상 효과

- **대박 확률**: 10배 증가
- **평균 시청 지속 시간**: 30초 → 50초 (완시청)
- **공유율**: 3~5배 증가
- **댓글 참여**: 활발

---

## 💡 참고사항

1. **대박 모드는 Double Entendre 전용**
   - 다른 시나리오 모드에서는 작동하지 않음
   - Double Entendre만 선택했을 때 활성화

2. **기존 시스템 유지**
   - 대박 모드 비활성화 시 기존 방식 그대로
   - 사용자가 선택 가능

3. **테스트 필수**
   - 추가 후 반드시 테스트
   - 여러 시나리오로 검증
