# v3.1.2와 v3.2.0 버전 비교 분석 보고서

## 🎯 요약

v3.2.0에서 왼쪽 패널의 테스트 버튼으로 대본을 생성할 때 LLM이 작성한 대본을 가져오지 못하는 문제의 원인을 파악하기 위해 두 버전을 비교 분석했습니다.

---

## 📁 파일 구조 차이점

### v3.1.2에만 존재하는 파일
- `services/storyService.ts` (29줄) - 대본 저장 로직
- `services/cineboardService.ts` (44줄) - Cineboard 작업 상태 저장/로드 로직

### v3.2.0에서 제거된 파일
- `services/storyService.ts` - ❌ 삭제됨
- `services/cineboardService.ts` - ❌ 삭제됨

---

## 🔍 주요 차이점 분석

### 1. **`storyService.ts` 파일 누락 (핵심 이슈)**

#### v3.1.2의 `storyService.ts`
```typescript
export async function saveStoryFile(payload: SaveStoryPayload): Promise<SaveStoryResponse> {
  const response = await fetch('http://localhost:3002/api/save-story', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload)
  });

  if (!response.ok) {
    const errorData = await response.json().catch(() => ({}));
    throw new Error(errorData.error || 'Failed to save story');
  }

  return response.json();
}
```

**역할:**
- 대본 데이터를 서버에 저장하는 헬퍼 함수
- `App.tsx`에서 `saveStoryFile()` 함수를 import하여 사용
- 서버 API `/api/save-story`를 호출하여 대본과 이미지 프롬프트를 파일로 저장

#### v3.2.0의 변경사항
- `storyService.ts` 파일이 완전히 제거됨
- `App.tsx`에서 `saveStoryFile` import가 없음
- 대신 App.tsx에서 직접 fetch 호출을 사용 (라인 749-759)

```typescript
// v3.2.0 App.tsx (라인 749-759)
fetch('http://localhost:3002/api/save-story', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    title: newStory.title,
    content: formattedContent,
    service: input.targetService || 'GEMINI'
  })
}).then(res => res.json())
  .then(data => console.log("Saved to file:", data.filename))
  .catch(err => console.error("Auto-save failed:", err));
```

**⚠️ 잠재적 문제:**
- 에러 처리가 `.catch()`로만 되어 있어 실패 시 조용히 실패함
- `folderName` 파라미터가 전달되지 않음 (v3.1.2에서는 `newStory._folderName`을 전달)

---

### 2. **`youtube-shorts-script-generator.tsx` 차이점**

#### Import 차이
**v3.1.2 (라인 7-8):**
```typescript
import { saveStoryFile } from './services/storyService';
import { buildGeneratedImageUrlCandidates } from './components/master-studio/services/historyImageLoader';
```

**v3.2.0:**
```typescript
// saveStoryFile import 없음
// buildGeneratedImageUrlCandidates import 없음
```

#### `enhanceScenePrompt` 함수의 `qualitySuffix` 차이

**v3.1.2 (라인 750-753):**
```typescript
const qualitySuffix = "Hyper-realistic 8K cinematic photograph, Magazine Cover Quality, Shallow depth of field, cinematic lighting, --ar 9:16";
if (!/hyper-realistic\\s*8k|magazine cover quality|--ar\\s*9:16/i.test(updated)) {
  updated += `, ${qualitySuffix}`;
}
```

**v3.2.0 (라인 748):**
```typescript
// qualitySuffix 추가 로직이 완전히 제거됨
```

**영향:**
- v3.2.0에서는 이미지 프롬프트에 품질 태그가 자동으로 추가되지 않음
- 이미지 품질이 저하될 가능성

---

### 3. **`App.tsx` - 대본 생성 및 저장 로직 차이**

#### v3.1.2 코드 (라인 653-659)
```typescript
saveStoryFile({
  title: newStory.title,
  content: formattedContent,
  service: input.targetService || 'GEMINI',
  folderName: newStory._folderName  // ✅ folderName 전달
}).then(data => console.log("Saved to file:", data.filename))
  .catch(err => console.error("Auto-save failed:", err));
```

#### v3.2.0 코드 (라인 749-759)
```typescript
fetch('http://localhost:3002/api/save-story', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    title: newStory.title,
    content: formattedContent,
    service: input.targetService || 'GEMINI'
    // ❌ folderName 누락
  })
}).then(res => res.json())
  .then(data => console.log("Saved to file:", data.filename))
  .catch(err => console.error("Auto-save failed:", err));
```

**주요 차이:**
- `folderName` 파라미터가 전달되지 않음
- 서버에서 올바른 폴더에 저장하지 못할 가능성

---

## 💡 해결 방안

### 옵션 1: v3.1.2의 `storyService.ts` 파일 복원 (권장)

1. **`storyService.ts` 파일 복사**
   ```bash
   copy "f:\projact\쇼츠대본생성기-v3.1.2\services\storyService.ts" "f:\projact\쇼츠대본생성기-v3.2.0\services\storyService.ts"
   ```

2. **`App.tsx`에 import 추가**
   ```typescript
   import { saveStoryFile } from './services/storyService';
   ```

3. **`App.tsx`의 fetch 호출을 `saveStoryFile`로 교체 (라인 749-759)**
   ```typescript
   saveStoryFile({
     title: newStory.title,
     content: formattedContent,
     service: input.targetService || 'GEMINI',
     folderName: newStory._folderName
   }).then(data => console.log("Saved to file:", data.filename))
     .catch(err => console.error("Auto-save failed:", err));
   ```

### 옵션 2: `App.tsx`의 fetch 호출에 `folderName` 추가

```typescript
fetch('http://localhost:3002/api/save-story', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    title: newStory.title,
    content: formattedContent,
    service: input.targetService || 'GEMINI',
    folderName: newStory._folderName  // ✅ 추가
  })
}).then(res => res.json())
  .then(data => console.log("Saved to file:", data.filename))
  .catch(err => console.error("Auto-save failed:", err));
```

### 옵션 3: `youtube-shorts-script-generator.tsx`에 `qualitySuffix` 복원

**라인 748 이후에 추가:**
```typescript
const qualitySuffix = "Hyper-realistic 8K cinematic photograph, Magazine Cover Quality, Shallow depth of field, cinematic lighting, --ar 9:16";
if (!/hyper-realistic\\s*8k|magazine cover quality|--ar\\s*9:16/i.test(updated)) {
  updated += `, ${qualitySuffix}`;
}
```

---

## 📋 체크리스트

- [ ] `services/storyService.ts` 파일 복원
- [ ] `App.tsx`에 `saveStoryFile` import 추가
- [ ] `App.tsx`의 대본 저장 로직 수정 (folderName 전달)
- [ ] `youtube-shorts-script-generator.tsx`의 `qualitySuffix` 로직 복원 (선택사항)
- [ ] 테스트 버튼으로 대본 생성 후 정상 동작 확인

---

## 🔗 관련 파일 경로

### v3.1.2
- [`services/storyService.ts`](file:///f:/projact/쇼츠대본생성기-v3.1.2/services/storyService.ts)
- [`App.tsx`](file:///f:/projact/쇼츠대본생성기-v3.1.2/App.tsx#L653-L659)
- [`youtube-shorts-script-generator.tsx`](file:///f:/projact/쇼츠대본생성기-v3.1.2/youtube-shorts-script-generator.tsx#L750-L753)

### v3.2.0
- [`App.tsx`](file:///f:/projact/쇼츠대본생성기-v3.2.0/App.tsx#L749-L759)
- [`youtube-shorts-script-generator.tsx`](file:///f:/projact/쇼츠대본생성기-v3.2.0/youtube-shorts-script-generator.tsx#L748)

---

## 📝 결론

**핵심 이슈:**
- v3.2.0에서 `storyService.ts` 파일이 제거되면서 대본 저장 로직이 불완전하게 변경됨
- `folderName` 파라미터가 전달되지 않아 서버에서 올바른 경로에 대본을 저장하지 못할 가능성
- 이미지 프롬프트의 품질 태그가 자동 추가되지 않음

**권장 해결책:**
1. v3.1.2의 `storyService.ts` 파일을 v3.2.0에 복사
2. `App.tsx`에서 해당 서비스를 import하고 사용
3. 필요시 `qualitySuffix` 로직도 복원
