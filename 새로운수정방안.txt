 지금 대본 생성 로직이 또다시 엉망으로 바뀐것같아 쇼츠랩에 ai 대본생성, 2단계 생성 의 대본만드는 로직을
비교분석해줘 대본의 품질은 어떤지 그리고 이미지프롬프트만들때 대본에 맞는 이미지프롬프트를 만들고 있는지 캐릭터의
의상을 일관성있게 모든씬에 적용시키고 있는지 2명,3명 나온때 일관성 있게 의상을 고정시켜주는지 주재에 맞는 배경을
설정하는지 모든 로직을 비교분석해줘 지금 총체적 난국이야 에이전트나 스킬즈로 꼼꼼히 비교분석해주고 오류나 개선점이
있으면 설명해줘 무슨말인지 알겠어?

● 네, 정확히 이해했습니다! 지금 쇼츠랩의 대본 생성 로직이 문제가 있다는 것이고, 두 가지 방식을 비교 분석해야 합니다:

  1. 쇼츠랩 AI 대본생성
  2. 2단계 생성

  비교할 핵심 항목:
  - 대본 품질
  - 이미지 프롬프트와 대본의 일치도
  - 캐릭터 의상 일관성 (모든 씬)
  - 다중 인물(2명, 3명) 의상 일관성
  - 주제에 맞는 배경 설정

  에이전트를 활용해서 꼼꼼히 분석하겠습니다.

● Explore(쇼츠랩 대본 생성 로직 탐색)
  ⎿  Done (15 tool uses · 43.3k tokens · 2m 15s)

● 훌륭한 분석입니다! 이제 다음 단계로 이미지 프롬프트 품질과 의상 일관성 문제를 깊이 분석하겠습니다.

● Explore(이미지 프롬프트 품질 및 의상 일관성 분석)
  ⎿  Done (19 tool uses · 70.2k tokens · 2m 15s)

● 완벽한 분석이 완료되었습니다! 이제 종합 리포트를 작성하겠습니다.

● 🚨 쇼츠대본 생성 로직 종합 분석 리포트

  📋 요약

  두 가지 대본 생성 방식을 심층 분석한 결과, 총체적인 난국이 맞습니다. 양쪽 모두 심각한 문제가 있습니다:

  - AI 대본생성: AI에게 규칙만 전달, 검증/강제 메커니즘 없음
  - 2단계 생성: Dynamic Outfit-Location Sync 미지원
  - 공통 문제: 구현한 determineOutfitForScene 함수를 실제로 사용하지 않음

  ---
  ⚠️ 치명적 문제점 5가지

  1️⃣ 의상 일관성 검증 부재 (가장 심각)

  AI 대본생성

  // labPromptBuilder.ts:1854
  4. **8개 모든 씬에서 identity/hair/body/outfit 문구 100% 동일**
  → AI에게 지시만 하고 검증하지 않음

  검증 함수 문제

  // ShortsLabPanel.tsx:2087-2091
  const hasPersonMarkers = /\[Person\s+\d+:/i.test(longPrompt);
  if (!hasPersonMarkers) {
    // 원샷만 의상 동기화
  } else {
    // 투샷/쓰리샷은 AI 믿음 (검증 없음!)
  }

  결과: 투샷/쓰리샷에서 AI가 의상을 다르게 쓰면 탐지 불가

  ---
  2️⃣ Dynamic Outfit-Location Sync 미작동

  함수는 구현됨

  // labPromptBuilder.ts:1552-1569
  export const determineOutfitForScene = (
    scriptLine: string,
    defaultOutfit: string,
    gender: 'female' | 'male'
  ): string => {
    const detectedLocation = detectLocationChange(scriptLine);
    if (!detectedLocation || detectedLocation === 'golf') {
      return defaultOutfit;
    }
    const newOutfit = getOutfitForLocation(detectedLocation, gender);
    return newOutfit;
  };

  하지만 호출하는 곳이 없음!

  검색 결과:
  - tests/creative_engine_verify.ts - 테스트에만 사용
  - 실제 생성 플로우 (handleAiGenerate, handleTwoStepGenerate)에서 호출 안 함

  결과: "골프장 → 집으로 돌아와" 씬에서 골프복 그대로

  ---
  3️⃣ 배경 일관성 강제 없음

  규칙만 있음

  // labPromptBuilder.ts:1860
  8. **배경은 1번 씬의 문구를 그대로 복붙** (장소 전환이 scriptLine에 명시된 경우만 변경 허용)

  검증 코드 없음

  - postProcessAiScenes에 배경 체크 로직 없음
  - AI가 무시하면 막을 방법 없음

  결과: 주제와 무관한 배경 등장 가능

  ---
  4️⃣ 투샷/쓰리샷 [Person X] 구분자 강제 안 됨

  AI 대본생성

  // labPromptBuilder.ts:1883-1910
  ✅ **투샷 예시** (2명) - [Person 1], [Person 2] 필수:
  → 예시만 제공, AI가 안 쓰면 검증 통과 못함

  2단계 생성은 안전

  // ShortsLabPanel.tsx:2006-2035
  const identityBlock = `[Person ${personIdx + 1}: ${identity}, ...]`;
  → 클라이언트가 강제 삽입

  결과: AI 대본생성에서 인물 혼동 가능

  ---
  5️⃣ 2단계 생성에 Dynamic Outfit Sync 규칙 누락

  buildManualSceneDecompositionPrompt (manualSceneBuilder.ts)

  // 라인 126-235
  // 의상 일관성 규칙은 있음 (라인 173-178)
  // Dynamic Outfit-Location Sync 규칙 없음!

  결과: 2단계 생성에서는 장소 변화 시 의상 변경 원천 불가능

  ---
  📊 상세 비교표

  | 항목        | AI 대본생성               | 2단계 생성                     | 문제 원인                       |
  |-----------|-----------------------|----------------------------|-----------------------------|
  | 프롬프트 빌더   | buildLabScriptPrompt  | buildLabScriptOnlyPrompt   | -                           |
  | API 호출    | 1회 (통합)               | 3회 (분리)                    | -                           |
  | 이미지 프롬프트  | AI 직접 생성              | 클라이언트 조합                   | -                           |
  | 의상 출처     | AI 지정 (lockedOutfits) | 랜덤 (buildAutoCharacterMap) | -                           |
  |           |                       |                            |                             |
  | 의상 일관성 보장 | ❌ AI 지시만              | ✅ 한 번 할당                   | validateAndFixPrompt 검증 부족
   |
  | 장소별 의상 변경 | ❌ 규칙만 있음              | ❌ 미지원                      | determineOutfitForScene 미사용
   |
  | 배경 일관성    | ❌ 검증 없음               | ❌ 검증 없음                    | 강제 메커니즘 없음
      |
  | 투샷/쓰리샷 구분 | ❌ 예시만                 | ✅ 강제 삽입                    | composeManualPrompt만 안전
  |
  | 캐릭터 정보 품질 | ✅ AI 생성 (문맥 이해)       | ⚠️ 자동 생성 (랜덤)              | -
   |
  | 대본 품질     | ✅ 높음                  | ⚠️ 중간                      | AI 통합 생성 vs 분리 생성           |

  ---
  🔍 코드 레벨 문제점

  문제 1: postProcessAiScenes가 의상 검증 안 함

  위치: ShortsLabPanel.tsx:953-1050

  export const postProcessAiScenes = (...) => {
    // buildCharacterInfoMap으로 characters 추출 (라인 969)
    // enhanceScenePrompt로 표정/카메라 추가 (라인 1000-1011)
    // validateAndFixPrompt 호출 (라인 1012)
    // applyAccessoriesToPrompt로 악세서리 추가 (라인 1024)

    // 🚨 문제: 의상 일관성 체크 없음!
    // 🚨 문제: 배경 일관성 체크 없음!
    // 🚨 문제: 장소별 의상 변경 적용 안 함!
  }

  문제 2: validateAndFixPrompt가 투샷/쓰리샷 검증 안 함

  위치: ShortsLabPanel.tsx:2083-2136

  export const validateAndFixPrompt = (...) => {
    const hasPersonMarkers = /\[Person\s+\d+:/i.test(longPrompt);

    if (!hasPersonMarkers) {
      // 원샷: identity, hair, body, outfit 동기화 ✅
    } else {
      // 투샷/쓰리샷: 아무것도 안 함 ❌
      // AI가 [Person 1], [Person 2]를 제대로 썼다고 가정
    }
  }

  개선 필요: 투샷/쓰리샷에서도 각 Person 블록 내 outfit 일관성 검증

  문제 3: determineOutfitForScene 호출 누락

  위치: 호출하는 곳이 없음

  필요한 위치:
  - postProcessAiScenes (AI 대본생성용)
  - handleManualSceneGeneration (2단계 생성용)

  // 추가해야 할 로직 예시
  scenesSource.forEach((scene, idx) => {
    const scriptLine = scene.scriptLine || scene.text;

    // 장소 변화 감지 및 의상 변경
    characters.forEach(char => {
      const newOutfit = determineOutfitForScene(
        scriptLine,
        char.outfit,
        char.gender
      );
      if (newOutfit !== char.outfit) {
        console.log(`[Scene ${idx}] 의상 변경: ${char.outfit} → ${newOutfit}`);
        char.outfit = newOutfit;
      }
    });

    // 이후 프롬프트 재생성
  });

  ---
  💡 개선안

  즉시 수정 필요 (Critical)

  1. postProcessAiScenes에 의상 검증 추가

  // ShortsLabPanel.tsx:953 이후 추가
  export const postProcessAiScenes = (...) => {
    // ... 기존 코드 ...

    // 🆕 의상 일관성 검증
    const outfitConsistency = validateOutfitConsistency(processedScenes, characterInfoMap);
    if (!outfitConsistency.isConsistent) {
      console.warn('[Outfit Inconsistency]', outfitConsistency.issues);
      // 강제 수정
      processedScenes = forceOutfitConsistency(processedScenes, characterInfoMap);
    }

    return processedScenes;
  };

  2. validateAndFixPrompt 투샷/쓰리샷 검증 추가

  // ShortsLabPanel.tsx:2083 수정
  if (hasPersonMarkers) {
    // 🆕 투샷/쓰리샷 검증
    characters.forEach((char, idx) => {
      const personRegex = new RegExp(`\\[Person ${idx + 1}:[^\\]]+wearing\\s+([^,\\]]+)`, 'i');
      const match = fixedPrompt.match(personRegex);
      if (match && match[1] !== char.outfit) {
        // 의상 불일치 발견 - 강제 수정
        fixedPrompt = fixedPrompt.replace(personRegex,
          `[Person ${idx + 1}: ... wearing ${char.outfit}`
        );
      }
    });
  }

  3. determineOutfitForScene 적용

  // ShortsLabPanel.tsx:1000 근처 추가
  processedScenes.forEach((scene, idx) => {
    const scriptLine = scene.scriptLine || '';

    // 🆕 장소별 의상 변경
    scene.characters?.forEach(charId => {
      const charMeta = characterInfoMap.get(charId);
      if (charMeta) {
        const newOutfit = determineOutfitForScene(
          scriptLine,
          charMeta.outfit,
          charMeta.gender
        );
        if (newOutfit !== charMeta.outfit) {
          // longPrompt에서 의상 교체
          scene.longPrompt = scene.longPrompt.replace(
            new RegExp(`wearing ${charMeta.outfit}`, 'gi'),
            `wearing ${newOutfit}`
          );
          charMeta.outfit = newOutfit; // 다음 씬을 위해 업데이트
        }
      }
    });
  });

  중기 개선 (Important)

  4. buildManualSceneDecompositionPrompt에 Dynamic Outfit Sync 규칙 추가

  // manualSceneBuilder.ts:126 수정
  export const buildManualSceneDecompositionPrompt = (...) => {
    return `
    ${기존 규칙}

    ## ⚠️ Dynamic Outfit-Location Sync
    - scriptLine에서 장소 변화 감지 시 의상 자동 변경
    - "집", "집으로" → Home Wear / Casual
    - "카페" → Casual Chic
    - "식당" → Smart Casual
    - 골프장 → 집: 골프복 벗고 Home Wear 필수!
    `;
  };

  5. 배경 일관성 강제

  // postProcessAiScenes에 추가
  const firstSceneBackground = extractBackground(processedScenes[0].longPrompt);
  processedScenes.forEach((scene, idx) => {
    if (idx > 0 && !hasLocationChange(scene.scriptLine)) {
      // 배경 강제 통일
      scene.longPrompt = replaceBackground(scene.longPrompt, firstSceneBackground);
    }
  });

  ---
  🎯 결론

  현재 상태

  - AI 대본생성: 품질은 높지만 방어 메커니즘 전무
  - 2단계 생성: 캐릭터 일관성은 나은데 Dynamic Outfit Sync 없음
  - 공통: 구현한 기능을 실제로 사용 안 함

  권장 조치

  1. 즉시: validateAndFixPrompt 투샷/쓰리샷 검증 추가
  2. 즉시: determineOutfitForScene 실제 적용
  3. 단기: 의상 일관성 강제 메커니즘 구축
  4. 중기: 배경 일관성 검증 추가
  5. 중기: 2단계 생성에 Dynamic Outfit Sync 지원

  지금 상태로는 두 방식 모두 신뢰할 수 없습니다. 개선 작업을 진행하시겠습니까?

> 다 한거야?

● 아닙니다! 분석만 끝났고 실제 수정은 아직 안 했습니다.

  현재까지:
  - ✅ 두 방식 비교 분석 완료
  - ✅ 문제점 5가지 발견
  - ✅ 개선안 제시

  아직 안 한 것:
  - ❌ 실제 코드 수정
  - ❌ 문제 해결

  지금 바로 수정 작업을 시작할까요?

  주요 수정 사항:
  1. validateAndFixPrompt - 투샷/쓰리샷 의상 검증 추가
  2. postProcessAiScenes - 의상 일관성 강제 + Dynamic Outfit Sync 적용
  3. determineOutfitForScene - 실제 호출 적용
  4. buildManualSceneDecompositionPrompt - Dynamic Outfit Sync 규칙 추가
  5. 배경 일관성 검증 추가