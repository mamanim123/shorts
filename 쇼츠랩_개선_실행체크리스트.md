# 쇼츠랩 개선 실행 체크리스트

작성일: 2026-06-13

이 문서는 쇼츠랩 개선 작업을 진행하면서 단계별로 상태를 체크하기 위한 실행용 체크리스트다.  
작업을 하나 진행할 때마다 이 문서의 상태를 업데이트한다.

상태 표기:

- `[ ]` 아직 시작 안 함
- `[~]` 진행 중
- `[x]` 완료
- `[!]` 보류 또는 주의 필요

## 0. 작업 운영 규칙

- [x] 기존 분석 문서 작성 완료: `쇼츠랩_버튼_로직_상세분석.md`
- [x] 의존성 안전 개선 계획 문서 작성 완료: `쇼츠랩_의존성_안전_개선_작업계획.md`
- [x] Git 사무실 커밋 반영 및 push 완료
- [x] 현재 기준 `npm run build` 성공 확인
- [ ] 작업 시작 전 항상 `git status --short --branch` 확인
- [ ] 작업 시작 전 변경 대상 파일 목록 확정
- [ ] 작업 완료 후 `npm run build` 실행
- [ ] 작업 완료 후 체크리스트 업데이트
- [ ] 작업 완료 후 필요 시 분석/계획 문서 업데이트

## 1. Phase 0 - 기준선 고정

목표: 개선 작업 전 기준 상태를 저장해서 변경 전후를 비교할 수 있게 한다.

- [ ] 현재 `origin/master` 최신 여부 확인
- [ ] 현재 로컬 변경 파일 확인
- [ ] 쇼츠랩 일반 생성 기준 샘플 확보
- [ ] V4 생성 기준 샘플 확보
- [ ] 수동 장면분해 기준 샘플 확보
- [ ] 기준 prompt 예시 저장 위치 결정
- [ ] 기준 빌드 결과 기록

완료 기준:

- 기준 샘플이 있어야 이후 변경이 실제 결과를 바꿨는지 비교할 수 있다.


    param($m)
    ($m.Groups[1].Value -replace "- \[[ ~]\]", "- [x]") + $m.Groups[2].Value
 - Prompt Trace 추가

목표: 최종 prompt가 어떤 단계를 거쳐 만들어졌는지 확인한다.

- [ ] `services/shorts-lab/promptTrace.ts` 추가
- [ ] `PromptTraceStep` 타입 정의
- [ ] AI 원본 scene 단계 기록
- [ ] 캐릭터 적용 전/후 단계 기록
- [ ] 의상 적용 후 단계 기록
- [ ] camera/shotType 보정 후 단계 기록
- [ ] 후처리 전/후 단계 기록
- [ ] 이미지 생성 직전 prompt 단계 기록
- [ ] 씬 상세 또는 개발용 패널에서 trace 확인
- [ ] 기존 prompt 값 변경 없음 확인
- [ ] `npm run build` 성공 확인

완료 기준:

- 특정 scene의 prompt 변화 과정을 사람이 추적할 수 있다.

## 4. Phase 3 - 최종 프롬프트 공통 파이프라인 Preview 추가

목표: 실제 prompt를 바꾸지 않고 공통 파이프라인 preview만 만든다.

- [ ] `services/shorts-lab/finalPromptPipeline.ts` 추가
- [ ] `FinalPromptInput` 타입 정의
- [ ] `FinalPromptResult` 타입 정의
- [ ] 기존 scene.prompt를 그대로 반환하는 preview 함수 추가
- [ ] diagnostics 연결
- [ ] prompt trace 연결
- [ ] feature flag 또는 내부 옵션 설계
- [ ] 일반 생성 결과와 preview 결과 비교
- [ ] V4 생성 결과와 preview 결과 비교
- [ ] 수동 장면분해 결과와 preview 결과 비교
- [ ] 기존 prompt 값 변경 없음 확인
- [ ] `npm run build` 성공 확인

완료 기준:

- 새 파이프라인이 기존 결과를 바꾸지 않고 비교 데이터만 만든다.

## 5. Phase 4 - 캐릭터/3면도 연결 검사기 추가

목표: scene의 캐릭터, 저장 캐릭터, 3면도, 의상, prompt 반영 여부를 한 번에 확인한다.

- [ ] `services/shorts-lab/characterConnectionDiagnostics.ts` 추가
- [ ] scene별 캐릭터 ID 목록 추출
- [ ] 슬롯 규칙 존재 여부 검사
- [ ] 저장 캐릭터 존재 여부 검사
- [ ] 3면도 참조 존재 여부 검사
- [ ] 의상 존재 여부 검사
- [ ] prompt 내부 반영 여부 검사
- [ ] 씬별 연결 상태 UI 표시
- [ ] 3면도 버튼 실행 후 연결 상태 확인
- [ ] `npm run build` 성공 확인

완료 기준:

- 3면도가 실제로 어느 scene/character에 연결됐는지 바로 알 수 있다.

## 6. Phase 5 - 버튼 이름과 버튼 조건 정리

목표: 버튼 이름과 실제 동작을 맞추고, 잘못된 disabled 조건을 줄인다.

- [ ] `AI 줄거리 생성` disabled 조건 확인
- [ ] 벤치마크 원문만 있을 때 생성 가능한지 확인
- [ ] `이 줄거리로 대본 생성` 이름 검토
- [ ] 일반 흐름에서 대본만 생성하는 경우 버튼명 수정 검토
- [ ] `AI 마스터 생성` 실제 동작과 이름 일치 여부 확인
- [ ] `V4 줄거리`와 V4 대본/씬 생성 버튼 구분 확인
- [ ] 버튼명 변경 시 UI 문구만 수정
- [ ] 버튼 로직 변경은 최소화
- [ ] `npm run build` 성공 확인

완료 기준:

- 버튼을 눌렀을 때 사용자가 기대하는 결과와 실제 결과가 맞아야 한다.

## 7. Phase 6 - 공통 파이프라인 일부 흐름 적용

목표: preview로 검증한 공통 파이프라인을 낮은 위험 흐름부터 실제 적용한다.

- [ ] 수동 장면분해 흐름 적용 후보 확인
- [ ] 수동 장면분해에 feature flag로 적용
- [ ] 기존 prompt와 새 prompt diff 확인
- [ ] 일반 마스터 흐름 적용 후보 확인
- [ ] 일반 마스터에 feature flag로 적용
- [ ] V4 적용 전 영향 범위 재검토
- [ ] V4에 feature flag로 적용
- [ ] 각 흐름별 rollback 방법 확인
- [ ] `npm run build` 성공 확인

완료 기준:

- 공통 파이프라인을 켜도 기존 기능을 즉시 깨지 않고 비교/복구할 수 있다.

## 8. Phase 7 - 실패 복구 버튼 추가

목표: 전체 재생성이 아니라 실패한 단계만 다시 실행할 수 있게 한다.

- [ ] `현재 대본으로 씬 분해 재시도` 버튼 설계
- [ ] `캐릭터/3면도 다시 적용` 버튼 설계
- [ ] `의상만 다시 배정` 버튼 설계
- [ ] `프롬프트만 다시 생성` 버튼 설계
- [ ] `이 씬만 이미지 재생성` 버튼 설계
- [ ] 기존 생성 버튼과 별도 핸들러로 구현
- [ ] 실패 상태별 활성/비활성 조건 정의
- [ ] `npm run build` 성공 확인

완료 기준:

- 실패한 단계만 다시 실행할 수 있어야 한다.

## 9. Phase 8 - JSON 파싱 디버그 패널 추가

목표: AI 응답 파싱 실패 원인을 UI에서 확인할 수 있게 한다.

- [ ] LLM 원문 응답 보관 방식 결정
- [ ] JSON 추출 전 텍스트 표시
- [ ] JSON 추출 결과 표시
- [ ] `jsonrepair` 적용 결과 표시
- [ ] normalize 후 scenes 표시
- [ ] 파싱 실패 에러 표시
- [ ] API 키/민감정보 필터링
- [ ] 기본 접힘 UI로 추가
- [ ] `npm run build` 성공 확인

완료 기준:

- 파싱 실패 시 어느 단계에서 깨졌는지 확인할 수 있다.

## 10. 추후 리팩토링

목표: 진단기와 파이프라인이 안정된 뒤 `ShortsLabPanel.tsx`를 안전하게 나눈다.

- [ ] 순수 함수부터 분리
- [ ] 타입 분리
- [ ] 진단/후처리 함수 분리
- [ ] 줄거리 흐름 훅 분리
- [ ] V4 흐름 훅 분리
- [ ] 캐릭터/3면도 훅 분리
- [ ] UI 컴포넌트 분리
- [ ] 기존 버튼 동작 회귀 확인
- [ ] `npm run build` 성공 확인

완료 기준:

- 버튼 로직을 찾기 쉬워지고, 수정 영향 범위를 좁힐 수 있어야 한다.

## 11. 현재 다음 작업 후보

가장 안전한 다음 작업:

- [ ] Phase 1 시작: `sceneDiagnostics.ts` 읽기 전용 진단기 추가

이유:

- 기존 생성 결과를 바꾸지 않는다.
- 의존성 충돌 위험이 낮다.
- 이후 공통 파이프라인과 3면도 검사기의 기반이 된다.

