# 쇼츠랩 버튼/생성/캐릭터/후처리 로직 상세 분석

작성일: 2026-06-13  
범위: 쇼츠랩 탭 중심 분석  
주요 파일:
- `components/ShortsLabPanel.tsx`
- `components/CharacterPanel.tsx`
- `components/ShortsIdentityCard.tsx`
- `services/labPromptBuilder.ts`
- `services/geminiService.ts`
- `services/manualSceneBuilder.ts`
- `services/jsonParse.ts`
- `services/shortsLabCharacterRulesManager.ts`
- `services/shortslab-v4/*`

## 1. 전체 구조 요약

쇼츠랩은 단순한 "대본 생성" 화면이 아니라 다음 기능이 한 화면 안에 결합된 복합 제작 도구다.

1. 주제 입력 및 줄거리 생성
2. 선택 줄거리 기반 대본 생성
3. V4 대본/씬/이미지 프롬프트 생성
4. AI 마스터 생성
5. 수동 대본 입력 후 씬 분해
6. 캐릭터/의상 규칙 관리
7. 저장 캐릭터 생성/수정/가져오기
8. 이미지 프롬프트 후처리
9. 이미지 생성, 외부 AI 전송, Genspark 전송
10. 비디오 프롬프트 생성 및 Grok 영상 생성
11. 폴더 저장/불러오기/이미지 히스토리 연동

현재 가장 큰 복잡도 원인은 `ShortsLabPanel.tsx` 하나에 UI, 상태, API 호출, 파싱, 후처리, 캐릭터 동기화, 저장 로직이 함께 들어가 있기 때문이다.

## 2. 핵심 버튼 흐름

| 버튼/기능 | 주요 핸들러 | 핵심 역할 | 결과 |
|---|---|---|---|
| AI 줄거리 생성 | `handleGenerateStorylines` | 주제/벤치마크 기반 줄거리 10개 생성 | `storylines`, `benchmarkAnalysis`, `scriptPhase='storylines'` |
| V4 줄거리 | `handleMasterGenerateV4` | V4 조건이 붙은 줄거리 10개 생성 | `isV4StoryMode=true`, `storylines` 표시 |
| 이 줄거리로 대본 생성 | `handleConfirmStoryline` | 일반/V4 모드 분기 | 일반은 대본만, V4는 대본+씬+프롬프트 |
| AI 마스터 생성 | `handleMasterGenerate` | 대본+씬 구조+lockedOutfits 생성 | `scriptInput`, `scenes`, `masterOutfitMap` |
| 씬 분해 & 프롬프트 생성 | `handleParseScenes` | 수동/입력 대본 씬 분해 | `scenes[].prompt` 생성 |
| 캐릭터 정보 적용 | `handleApplyCharacterInfo` | 캐릭터 규칙+의상으로 최종 이미지 프롬프트 재생성 | `scene.prompt` 재작성 |
| 이미지 생성 | `handleGenerateImage` | Gemini 이미지 직접 생성 및 저장 | `scene.imageUrl`, `scene.seed` |
| AI 생성 | `handleForwardPromptToImageAI` | 외부 이미지 AI로 prompt 전송 | 저장 이미지 URL 반영 |
| Genspark 생성 | `handleForwardPromptToImageAI(..., 'GENSPARK')` | Genspark로 prompt 전송 | 저장 이미지 URL 반영 |
| 스마트 비디오 프롬프트 생성 | `handleRefineVideoPrompt` | scene prompt를 영상용 prompt로 변환 | `scene.videoPrompt` |
| Grok 생성 | `handleGenerateSceneVideo` | Grok 영상 생성 API 호출 | `scene.videoUrl` |

## 3. AI 줄거리 생성 흐름

버튼: `AI 줄거리 생성`  
핸들러: `handleGenerateStorylines`

흐름:
1. `aiTopic` 또는 `benchmarkSource`가 있는지 검사한다.
2. `isV4StoryMode=false`로 설정한다.
3. `generateBenchmarkStorylinePackage(aiTopic, benchmarkSource)`를 호출한다.
4. 내부에서 `http://localhost:3002/api/generate/raw`를 호출한다.
5. 결과 JSON에서 `analysis`, `storylines`를 파싱한다.
6. 첫 번째 줄거리를 선택 상태로 만들고 `scriptPhase='storylines'`로 바꾼다.

프롬프트 생성 위치:
- `services/geminiService.ts`의 `generateBenchmarkStorylinePackage`

요구 출력:
- `analysis.sourceSummary`
- `analysis.hookPattern`
- `analysis.narrativeStructure`
- `analysis.toneStyle`
- `storylines[].title`
- `storylines[].content`
- `storylines[].hook`
- `storylines[].twist`

주의점:
- 핸들러는 `aiTopic` 또는 `benchmarkSource`만 있어도 허용한다.
- 하지만 버튼 disabled 조건은 `!aiTopic.trim()` 기준이다.
- 따라서 벤치마크 설명만 입력한 상태에서는 핸들러상 가능하지만 UI 버튼은 비활성화될 수 있다.

개선 제안:
- disabled 조건을 `!aiTopic.trim() && !benchmarkSource.trim()`로 맞추는 것이 자연스럽다.
- 줄거리 생성 서비스는 현재 `service: 'GEMINI'` 고정이다. `targetService`를 반영하면 선택한 AI 서비스와 UI 의미가 일치한다.

## 4. 일반 줄거리 선택 후 대본 생성

버튼: `이 줄거리로 대본 생성`  
핸들러: `handleConfirmStoryline`

일반 모드 흐름:
1. 선택된 줄거리 제목과 편집된 줄거리 본문을 합쳐 `storyContext`를 만든다.
2. `isV4StoryMode`가 false면 `handleAiGenerateWithContext(storyContext)`로 이동한다.
3. `buildLabScriptOnlyPrompt`로 대본 전용 프롬프트를 만든다.
4. `/api/generate/raw`로 전송한다.
5. 응답에서 `scriptLines` 또는 `scriptBody`를 추출한다.
6. `scriptInput`에 대본만 넣고 `scenes`는 비운다.
7. 사용자는 이후 `씬 분해 & 프롬프트 생성`을 눌러야 이미지 프롬프트를 얻는다.

프롬프트 빌더:
- `services/labPromptBuilder.ts`의 `buildLabScriptOnlyPrompt`

입력:
- `topic`
- `genre`
- `targetAge`
- `gender`
- `genreGuideOverride`
- `legacyGenrePrompt`
- `characterSlotMode`
- `additionalContext`

중요 규칙:
- 대본 전용 생성이므로 `scenes`, `longPrompt`, `shortPrompt`를 생성하지 말라고 지시한다.

문제점:
- 일반 줄거리 흐름은 이름이 "대본 생성"이라 정확하지만, 사용자는 AI 줄거리 생성 후 곧바로 이미지 프롬프트까지 만들어질 것으로 오해할 수 있다.

개선 제안:
- 버튼명을 `이 줄거리로 대본만 생성` 또는 생성 후 토스트에 `다음 단계: 씬 분해 & 프롬프트 생성`을 더 명확히 표시한다.
- 일반 흐름에도 "대본+씬+프롬프트까지 자동 진행" 옵션을 추가하면 클릭 수를 줄일 수 있다.

## 5. V4 줄거리 및 V4 생성 흐름

버튼: `V4 줄거리`  
핸들러: `handleMasterGenerateV4`

V4 줄거리 흐름:
1. `generateV4StorylinePackage` 호출
2. 내부에서 `buildV4ConditionedTopic`으로 기존 주제에 V4 조건을 붙인다.
3. 기존 `generateBenchmarkStorylinePackage`를 재사용한다.
4. 결과를 `storylines`에 넣고 `isV4StoryMode=true`로 설정한다.

V4 조건:
- 장르
- 타겟 연령
- 주인공 성별
- 쇼츠용 대본으로 확장하기 좋은 줄거리
- 장면별 이미지화가 쉬운 행동/소품/장소 포함

V4 줄거리 선택 후 흐름:
1. `handleConfirmStoryline`
2. `isV4StoryMode=true`이므로 `handleV4GenerateFromStoryline`
3. `runShortsLabV4FromStoryContext`
4. `generateV4ScriptOnly`
5. `generateV4SceneAssignment`
6. `resolveV4Outfits`
7. `buildV4Scenes`
8. `validateV4Scenes`
9. `saveV4Result`
10. `scriptInput`, `scenes`, `masterOutfitMap`, `currentFolderName` 반영

V4 이미지 프롬프트 조립:
- `services/shortslab-v4/v4PromptFactory.ts`의 `buildV4Scenes`
- 캐릭터 규칙에서 face, hair, body, skinTone, signatureFeatures, style을 가져온다.
- 의상은 `outfitMap`에서 가져온다.
- 씬의 `cameraAngle`, `action`, `background`와 합친다.
- 끝에 기술 태그를 붙인다.

V4의 장점:
- 흐름이 `services/shortslab-v4` 폴더로 분리되어 있어 유지보수가 더 쉽다.
- 대본 생성, 씬 배정, 의상 결정, 프롬프트 조립이 파일 단위로 나뉘어 있다.

문제점:
- V4 줄거리도 내부적으로 기존 줄거리 생성기를 재사용하므로, 실제 V4만의 차별화는 `conditionedTopic` 문자열에 많이 의존한다.
- V4의 이미지 프롬프트 조립은 기존 `applyCharacterInfoToScenes`보다 단순하다. 다중 인물 분리, anti-swap, winter accessory, prefix/suffix 같은 후처리 일부가 약할 수 있다.

개선 제안:
- V4에도 공통 후처리 파이프라인을 붙여 V4와 기존 흐름의 결과 품질 차이를 줄인다.
- V4 전용 storyline prompt를 독립 함수로 분리해 기존 `generateBenchmarkStorylinePackage` 의존도를 낮춘다.

## 6. AI 마스터 생성 흐름

버튼: `AI 마스터 생성`  
핸들러: `handleMasterGenerate`

흐름:
1. `/api/outfits`에서 의상 목록을 불러온다.
2. `getCharacterRules()`로 현재 캐릭터 규칙을 불러온다.
3. `buildSimplifiedMasterPrompt`로 단일 Pass 프롬프트를 만든다.
4. `/api/generate/raw`로 전송한다.
5. JSON을 파싱한다.
6. `scriptBody` 또는 `scriptLines`를 정규화한다.
7. `lockedOutfits`를 정규화한다.
8. `characterIds`를 정규화한다.
9. `rebalanceMasterSceneCharacterMix`로 원샷/투샷/쓰리샷 구성을 보정한다.
10. 누락된 의상을 `assignOutfitsToCharacters`로 보완한다.
11. `masterCharacterProfiles`와 `masterOutfitMap`을 만든다.
12. `scriptInput`과 `scenes`를 세팅한다.

중요:
- AI 마스터 생성 직후 `scene.prompt`가 완성되지 않을 수 있다.
- 코드에 "프롬프트는 아직 생성하지 않음 - 버튼 클릭 시 생성" 주석이 있다.
- 최종 프롬프트는 `캐릭터 정보 적용` 버튼 또는 자동 적용 경로에서 만들어진다.

문제점:
- `AI 마스터 생성`이라는 버튼명만 보면 대본+씬+이미지 프롬프트가 모두 완성될 것처럼 보인다.
- 실제로는 씬 구조와 의상 lock까지 만들고, 최종 프롬프트는 별도 적용 단계가 필요하다.

개선 제안:
- 버튼명을 `AI 마스터 구조 생성`으로 바꾸거나, 생성 완료 후 `캐릭터 정보 적용` 버튼을 더 강조한다.
- 생성 완료 후 자동으로 `handleApplyCharacterInfo`를 실행하는 옵션을 제공한다.

## 7. 수동 대본 씬 분해 흐름

버튼: `씬 분해 & 프롬프트 생성`  
핸들러: `handleParseScenes`

분기:
- `activeTab === 'manual'`이면 `handleManualSceneGeneration`
- 아니면 `handleMasterStyleSceneGeneration`

수동 모드 `handleManualSceneGeneration` 흐름:
1. 대본에서 script line을 추출한다.
2. 성별을 추론한다.
3. `buildCharacterExtractionPrompt`로 인물 추출 프롬프트 생성
4. `/api/generate/raw` 호출
5. `parseCharacterExtractionResponse`로 인물 JSON 파싱
6. 추출 인물을 슬롯에 매핑한다.
7. `buildManualSceneDecompositionPrompt`로 씬 분해 프롬프트 생성
8. `/api/generate/raw` 호출
9. `parseManualSceneDecompositionResponse`로 씬 JSON 파싱
10. `lockedOutfits`를 반영한다.
11. `buildAutoCharacterMap`으로 캐릭터 맵 생성
12. 겨울 악세서리 옵션 적용
13. 씬마다 prompt 조립
14. 후처리 적용
15. `scenes` 상태에 반영

수동 인물 분석 관련 버튼:
- `AI 인물 분석`: `handleManualAICharacterAnalysis`
- `누락 인물 찾기`: `handleManualFindMissing`
- `캐릭터 추가`: `addManualIdentity`
- 인물 고정 토글: `manualIdentityLockEnabled`

## 8. 캐릭터 생성/수정/가져오기 로직

캐릭터 로직은 크게 3곳에 나뉘어 있다.

### 8.1 CharacterPanel의 저장 캐릭터 생성/수정

파일: `components/CharacterPanel.tsx`

탭:
- `캐릭터선택`
- `캐릭터관리`
- `의상추출`

핵심 함수:
- `handleSaveCharacter`
- `handleDeleteCharacter`
- `saveCharactersToBE`

새 캐릭터 저장:
1. 이름이 비어 있으면 저장하지 않는다.
2. body 기반으로 `generateStyleFromBody`를 호출해 style을 자동 생성한다.
3. 새 캐릭터면 `char-${Date.now()}` ID를 만든다.
4. `saveCharacters()`로 서버 저장 캐릭터 목록에 저장한다.
5. `onCharacterAdded` 콜백을 호출한다.
6. 입력 폼을 초기화하고 `캐릭터선택` 탭으로 돌아간다.

기존 캐릭터 수정:
1. `selectedCharacterId`가 있으면 기존 캐릭터를 찾는다.
2. `newCharacter`, `bodyTuning` 값을 덮어쓴다.
3. `saveCharacters()`로 저장한다.
4. 토스트로 수정 완료를 알린다.

### 8.2 저장 캐릭터를 쇼츠랩 슬롯 규칙에 동기화

파일: `components/ShortsLabPanel.tsx`

핵심 함수:
- `handleCharacterAddedToRules`

흐름:
1. 새 캐릭터 성별에 따라 `addFemaleCharacter` 또는 `addMaleCharacter`로 슬롯을 추가한다.
2. 추가된 마지막 슬롯 ID를 가져온다.
3. `shortsLabCharacterRulesManager.importCharacter(char, targetSlotId)`를 호출한다.
4. 캐릭터 규칙을 새로고침한다.

### 8.3 캐릭터/의상 규칙 관리

파일:
- `components/ShortsLabPanel.tsx`
- `services/shortsLabCharacterRulesManager.ts`

규칙 필드:
- `id`
- `characterId`
- `name`
- `identity`
- `hair`
- `body`
- `style`
- `outfitFit`
- `face`
- `skinTone`
- `signatureFeatures`
- `bustDescription`
- `heightDescription`
- `referenceViewPreference`
- `preserveIdentityOnly`
- `isFixedAge`
- `fixedAge`

추가:
- `addFemaleCharacter`
- `addMaleCharacter`

삭제:
- `deleteFemaleCharacter`
- `deleteMaleCharacter`
- `WomanD`는 캐디라 삭제 방지
- 성별별 최소 1개 유지

수정:
- UI 입력 변경은 `updateCharacterRulesField`가 `characterRulesState`만 바꾸고 dirty 상태를 켠다.
- 실제 저장은 `handleCharacterRulesSave` 또는 `handleSingleCharacterSave`가 수행한다.
- 저장 시 `updateCharacterRules`를 호출한다.
- 저장 후 `masterCharacterProfiles`에도 강제 동기화한다.
- 서버 저장 캐릭터 목록도 `fetchCharacters()` 후 매칭되는 캐릭터를 업데이트하고 `saveCharacters()`로 다시 저장한다.

가져오기:
- `handleOpenImportModal`이 저장 캐릭터 목록을 불러오고 성별로 필터링한다.
- `handleImportCharacter`가 선택 캐릭터를 현재 슬롯에 import한다.
- 내부적으로 `convertCharacterToSlotRule`이 `CharacterItem`을 `CharacterSlotRule`로 변환한다.

문제점:
- "캐릭터 저장" 버튼이 개별 저장처럼 보이지만 실제로는 전체 characterRulesState 저장이다.
- CharacterPanel의 캐릭터 저장과 쇼츠랩 캐릭터 규칙 저장이 별도 개념이라 사용자가 혼동할 수 있다.
- 저장 캐릭터, 슬롯 규칙, 마스터 프로필, 캐스팅 이미지가 서로 동기화되는 위치가 여러 군데다.

개선 제안:
- 캐릭터 데이터 계층을 문서상 명확히 구분한다.
  - 저장 캐릭터: 서버의 캐릭터 라이브러리
  - 슬롯 규칙: 쇼츠랩 WomanA/ManA 등에 붙는 프롬프트 규칙
  - 마스터 프로필: 현재 생성 결과 미리보기용 캐릭터 프로필
  - 캐스팅 이미지: 이미지 생성 시 참조 이미지
- UI에도 이 구분을 표시하면 수정 실수를 줄일 수 있다.

## 9. 최종 이미지 프롬프트 조립

최종 이미지 프롬프트는 하나의 경로만 있는 것이 아니다.

주요 경로:
1. V4: `buildV4Scenes`
2. AI 마스터/캐릭터 적용: `applyCharacterInfoToScenes`
3. 수동 씬 분해: `composeManualPrompt` + `validateAndFixPrompt` + 후처리
4. 일반 서비스 `generateStory`: `normalizeManualScenePrompts` 또는 보강 함수들

### 9.1 applyCharacterInfoToScenes

파일: `services/labPromptBuilder.ts`

입력:
- `scenes`
- `characterRules`
- `outfitMap`
- `accessoryMap`
- `forceWinterBackground`
- `targetAge`

조립 요소:
- 표정 힌트
- 캐릭터 블록
- action
- scene context
- camera block
- full body hint
- background
- color lock
- multi-person separation
- anti-swap
- no text/no letters
- quality tags

특징:
- 여성은 `A stunning Korean woman`
- 남성은 `A handsome Korean man`
- 나이대는 `convertAgeToEnglish`로 변환
- 의상은 `outfitMap`에서 가져온다.
- 다중 인물일 때 `Person 1`, `Person 2`로 분리한다.
- 같은 얼굴/의상/자세가 섞이는 문제를 줄이기 위한 anti-swap 문구를 붙인다.

### 9.2 수동 씬 분해 후처리 조립

수동 씬 분해 후 실제 prompt는 다음 순서로 만들어진다.

1. LLM이 만든 `scene.longPrompt`, `scene.summary`, `scene.action`, `scene.background`를 수집한다.
2. `composeManualPrompt`로 캐릭터/의상/표정/카메라를 강제 삽입한다.
3. `validateAndFixPrompt`로 Person 블록과 샷 타입을 검증/보정한다.
4. 겨울 악세서리 옵션이 켜져 있으면 `applyWinterLookToExistingPrompt`를 적용한다.
5. `applyAccessoriesToPrompt`로 캐릭터별 악세서리를 붙인다.
6. `useSafeGlamour`가 켜져 있으면 `enhancePromptWithSafeGlamour`를 적용한다.
7. `applyCleanupPatterns`로 금지/삭제 문구를 제거한다.
8. `applyPostProcessAffixes`로 prefix/suffix를 붙인다.

## 10. 파싱 로직

### 10.1 공통 JSON 파서

파일: `services/jsonParse.ts`

핵심 함수:
- `parseJsonFromText`

순서:
1. ```json 코드블록 제거
2. 텍스트 안에서 가장 마지막 유효 JSON 객체/배열 후보 추출
3. 특정 key 값 내부의 따옴표 보정
4. `JSON.parse`
5. 실패 시 `preprocessJson`
6. 실패 시 `jsonrepair`
7. 실패 시 `preprocessJson + jsonrepair`
8. 그래도 실패하면 `null`

장점:
- LLM이 코드블록, 설명문, 깨진 JSON을 섞어도 상당히 복구한다.

문제점:
- 실패했을 때 어떤 단계에서 왜 실패했는지 UI에 노출되지 않는다.
- "마지막 JSON 후보"를 우선 추출하기 때문에 응답에 예시 JSON과 실제 JSON이 함께 있을 경우 의도와 다를 수 있다.

개선 제안:
- 파싱 실패 리포트를 저장하거나 UI에 "원본 응답 보기" 버튼을 제공한다.
- 파싱 성공 시에도 사용한 JSON 후보 일부를 debug 파일로 저장하면 추적이 쉬워진다.

### 10.2 수동 인물 파싱

파일: `services/manualSceneBuilder.ts`

함수:
- `parseCharacterExtractionResponse`

출력:
- `characters[]`
- `lineCharacterNames[]`

역할:
- LLM이 추출한 인물명, 성별, 역할을 정규화한다.
- 각 대본 라인에 등장하는 인물명을 추출한다.

### 10.3 수동 씬 분해 파싱

파일: `services/manualSceneBuilder.ts`

함수:
- `parseManualSceneDecompositionResponse`

출력:
- `title`
- `lockedOutfits`
- `scenes[]`

scene 필드:
- `sceneNumber`
- `scriptLine`
- `summary`
- `action`
- `background`
- `shotType`
- `cameraAngle`
- `shortPrompt`
- `longPrompt`
- `negativePrompt`
- `characterIds`

## 11. 장르별 후처리 설정

후처리 설정은 장르 관리 모달 안에서 관리된다.

저장:
- `handlePostProcessSave`

설정 항목:
- `enabled`
- `skipIdentityInjection`
- `useSafeGlamour`
- `customPrefix`
- `customSuffix`
- `cleanupPatterns`

UI에 적힌 실제 적용 순서:
1. 후처리 ON 여부
2. 캐릭터/의상 고정·검증
3. 겨울 악세서리
4. Safe Glamour
5. 클린업 패턴
6. Prefix/Suffix

중요 동작:
- 후처리 OFF면 LLM 원본 프롬프트를 그대로 사용한다.
- 캐릭터/의상 고정을 끄면 씬마다 의상이 바뀌는 문제가 다시 생길 수 있다고 UI에 경고가 있다.

문제점:
- 후처리 설정이 장르별로 존재하지만, 어떤 생성 흐름에 100% 적용되는지 분산되어 있다.
- V4 경로에는 기존 후처리와 동일한 단계가 완전히 붙어 있지 않다.

개선 제안:
- `postProcessPromptPipeline(scene, context)` 같은 공통 함수로 분리한다.
- V4, 수동 씬 분해, AI 마스터 모두 같은 최종 후처리 함수를 타게 만든다.

## 12. 이미지 생성/전송 로직

### 12.1 이미지 생성

핸들러:
- `handleGenerateImage`

흐름:
1. seed 결정
2. scene의 캐스팅 참조 이미지 확인
3. `getScenePromptForGeneration`으로 최종 전송 prompt 획득
4. 나이/의상/악세서리 추가 보정
5. `generateImage`로 Gemini 이미지 생성
6. 참조 이미지가 있으면 `editImage`로 캐릭터 얼굴/정체성 보정
7. `/api/save-image`로 저장
8. `scene.imageUrl`, `scene.seed` 반영

### 12.2 외부 AI 전송

핸들러:
- `handleForwardPromptToImageAI`

흐름:
1. 서비스 결정: `GEMINI` 또는 `GENSPARK`
2. scene prompt 재계산
3. `/api/image/ai-generate` 호출
4. 성공 시 저장 이미지 URL을 scene에 반영

문제점:
- `handleGenerateImage`와 `handleForwardPromptToImageAI` 모두 `getScenePromptForGeneration`을 호출하지만, 후속 보정과 저장 경로가 다르다.

개선 제안:
- 이미지 생성 전용 `resolveFinalImagePrompt(scene)` 함수를 만들고 모든 이미지/외부전송 경로에서 같은 결과를 쓰게 한다.

## 13. 확인된 문제점

1. `ShortsLabPanel.tsx`가 너무 크다.
   - UI, 상태, API, 파싱, 후처리, 캐릭터 동기화가 한 파일에 섞여 있다.

2. 버튼명이 실제 동작과 다소 다르다.
   - `AI 마스터 생성`은 최종 프롬프트까지 항상 완성하는 버튼이 아니다.
   - 일반 `이 줄거리로 대본 생성`은 대본만 만들고 이미지 프롬프트는 별도 단계다.

3. 줄거리 생성 disabled 조건과 핸들러 조건이 다르다.
   - 핸들러는 `aiTopic` 또는 `benchmarkSource`를 허용한다.
   - 버튼은 `aiTopic`이 없으면 막는다.

4. 일반, V4, 수동, AI 마스터의 최종 prompt 조립 방식이 서로 다르다.
   - 유지보수 시 한쪽만 고치고 다른 경로는 그대로 남을 가능성이 높다.

5. 캐릭터 데이터 계층이 많다.
   - 저장 캐릭터
   - 슬롯 규칙
   - 마스터 프로필
   - 캐스팅 참조 이미지
   - 수동 인물 고정

6. 캐릭터 저장/동기화가 여러 위치에서 일어난다.
   - CharacterPanel 저장
   - characterRules 저장
   - masterCharacterProfiles 동기화
   - 서버 캐릭터 목록 동기화

7. 후처리 설정의 적용 범위가 흐름별로 다르다.
   - 특히 V4와 기존 수동/마스터 경로의 후처리 일관성이 약하다.

8. 파싱 실패 디버깅이 어렵다.
   - 원본 응답, 추출 JSON, repair 결과, 최종 정규화 결과를 한눈에 볼 수 있는 화면이 없다.

9. `캐릭터 저장` 버튼이 개별 저장처럼 보이지만 실제로는 전체 규칙 저장을 호출한다.

10. 일부 오래된 흐름이 남아 있다.
   - `handleMasterGenerate_OLD`, 2-Pass 관련 흐름 등은 명확히 "백업/레거시" 처리하지 않으면 수정자가 헷갈릴 수 있다.

## 14. 우선 개선 추천

### 1순위: 문서화와 분리

추천:
- `ShortsLabPanel.tsx`를 바로 대규모 리팩터링하기보다 먼저 기능맵을 고정한다.
- 이 문서를 기준으로 버튼별 소유 파일과 데이터 흐름을 분리한다.

분리 후보:
- `useShortsLabStoryGeneration`
- `useShortsLabSceneParsing`
- `useShortsLabCharacterSync`
- `useShortsLabImageGeneration`
- `shortsLabPostProcessPipeline.ts`
- `shortsLabPromptAssembler.ts`

### 2순위: 공통 최종 프롬프트 파이프라인

현재는 흐름별로 prompt 조립 방식이 다르다.

추천 공통 함수:
- `buildFinalScenePrompt(scene, context)`
- `postProcessFinalPrompt(prompt, context)`
- `resolveFinalImagePrompt(scene, settings)`

효과:
- V4, AI 마스터, 수동 씬 분해, 이미지 생성 버튼이 같은 결과를 사용한다.
- 특정 문제를 한 곳에서 고칠 수 있다.

### 3순위: 캐릭터 데이터 계층 정리

추천:
- UI에 캐릭터 데이터 종류를 명확히 표시한다.
  - "저장 캐릭터"
  - "쇼츠랩 슬롯 규칙"
  - "현재 대본 마스터 프로필"
  - "이미지 생성 캐스팅 참조"

추가하면 좋은 기능:
- "현재 씬이 어떤 캐릭터 정보를 실제로 쓰는지 보기"
- "저장 캐릭터와 슬롯 규칙 차이 비교"
- "이 슬롯을 저장 캐릭터로 다시 동기화"

### 4순위: 파싱 디버그 패널

추가하면 유용한 기능:
- LLM 원본 응답 보기
- 추출된 JSON 보기
- jsonrepair 적용 결과 보기
- 최종 scenes 배열 보기
- 파싱 실패 이유 보기
- scene별 누락 필드 표시

효과:
- "왜 대본은 나왔는데 이미지 프롬프트가 이상한가"를 빠르게 찾을 수 있다.

### 5순위: 버튼명/상태 개선

추천 변경:
- `AI 줄거리 생성` → `일반 줄거리 10개`
- `V4 줄거리` → `V4 줄거리 10개`
- 일반 모드 `이 줄거리로 대본 생성` → `이 줄거리로 대본만 생성`
- V4 모드 `이 줄거리로 대본 생성` → `V4 대본+씬+프롬프트 생성`
- `AI 마스터 생성` → `AI 마스터 구조 생성`

## 15. 추가하면 유용한 기능 추천

1. 생성 흐름 타임라인
   - "줄거리 생성 완료 → 대본 생성 완료 → 씬 분해 완료 → 캐릭터 적용 완료 → 이미지 생성 완료"를 단계별로 표시한다.

2. 씬별 프롬프트 diff 보기
   - LLM 원본 prompt
   - 캐릭터 적용 후 prompt
   - 후처리 후 prompt
   - 이미지 생성 직전 prompt

3. 캐릭터 슬롯 검사기
   - 모든 씬의 `characterIds`가 현재 규칙에 존재하는지 검사한다.
   - 삭제된 캐릭터 슬롯이 scene에 남아 있는지 표시한다.

4. 의상 잠금 검사기
   - 모든 등장 캐릭터가 `lockedOutfits` 또는 `outfitMap`을 갖는지 확인한다.
   - 같은 의상이 중복되는 경우 경고한다.

5. V4/기존 흐름 비교 버튼
   - 같은 주제로 일반 마스터와 V4 결과의 scene count, characterIds, prompt 길이, 후처리 적용 여부를 비교한다.

6. 실패 복구 버튼
   - 대본은 성공했지만 씬 분해 실패 시 "현재 대본으로 씬 분해 재시도"
   - 씬은 성공했지만 프롬프트가 비었을 때 "캐릭터 정보 적용 재시도"
   - 이미지 생성 실패 시 "동일 seed로 재시도"

7. 프롬프트 품질 점검
   - prompt 안에 금지어, 텍스트 생성 위험, 캐릭터 누락, 의상 누락, 배경 누락, 카메라 누락 여부를 자동 체크한다.

8. 캐릭터 동기화 상태 표시
   - 저장 캐릭터와 슬롯 규칙이 다른 경우 "불일치" 표시
   - 클릭하면 어떤 필드가 다른지 보여준다.

## 16. 수정 시 추천 진입점

줄거리 생성 문제:
- `services/geminiService.ts`
- `generateBenchmarkStorylinePackage`

일반 대본 생성 문제:
- `services/labPromptBuilder.ts`
- `buildLabScriptOnlyPrompt`
- `services/shortsLabStep2PromptRulesDefaults.ts`

V4 문제:
- `services/shortslab-v4/v4FlowService.ts`
- `services/shortslab-v4/v4ScriptService.ts`
- `services/shortslab-v4/v4SceneAssignmentService.ts`
- `services/shortslab-v4/v4OutfitService.ts`
- `services/shortslab-v4/v4PromptFactory.ts`

캐릭터 생성/수정 문제:
- `components/CharacterPanel.tsx`
- `services/characterService.ts`

캐릭터 슬롯 규칙 문제:
- `components/ShortsLabPanel.tsx`
- `services/shortsLabCharacterRulesManager.ts`
- `services/shortsLabCharacterRulesDefaults.ts`

최종 이미지 프롬프트 문제:
- `services/labPromptBuilder.ts`
- `applyCharacterInfoToScenes`
- `validateAndFixPrompt`
- `components/ShortsLabPanel.tsx`의 수동 후처리 구간

파싱 문제:
- `services/jsonParse.ts`
- `services/manualSceneBuilder.ts`

후처리 문제:
- `components/ShortsLabPanel.tsx`
- `applyCleanupPatterns`
- `applyPostProcessAffixes`
- `postProcessAiScenes`
- 장르별 `postProcessConfig`

## 17. 루트 폴더 정리 기록 - 2026-06-13

요청:
- 메인 폴더에 파일이 너무 많아 의존성 확인 후 깔끔하게 정리.
- 삭제보다는 나중에 확인 가능한 방식으로 보관.

처리 방식:
- 앱 실행에 필요한 핵심 파일과 폴더는 유지했다.
- 백업, 임시 스크립트, 디버그 산출물, 테스트 산출물, 분석 문서, 텍스트 자료는 삭제하지 않고 보관 폴더로 이동했다.
- 의존성은 `npm prune`으로 `package.json`에 없는 extraneous 패키지만 정리했다.

유지한 핵심 파일/폴더:
- `App.tsx`
- `index.tsx`
- `index.html`
- `package.json`
- `package-lock.json`
- `vite.config.ts`
- `tsconfig.json`
- `types.ts`
- `constants.ts`
- `engine_config.json`
- `engine_config.template`
- `components/`
- `services/`
- `hooks/`
- `server/`
- `utils/`
- `public/`
- `style_templates/`
- `generated_scripts/`
- `longform_sessions/`
- `ai_studio_bundle/`
- `puppeteer_user_data/`

정리 결과:
- 루트 일반 파일 수: 약 `314개`에서 `37개`로 감소.
- 백업/임시/디버그/테스트 산출물 이동: `backups/root_cleanup_20260613/`
- 분석 문서/텍스트/HTML 이동: `docs/root_archive_20260613/`
- 백업 정리 파일 수: `129개`
- 문서 정리 파일 수: `110개`
- 삭제한 파일: 없음.

의존성 정리:
- 실행 명령: `npm prune`
- 결과: extraneous 패키지 `33개` 제거.
- `npm ls --depth=0` 확인 결과 extraneous 패키지 없음.
- `node_modules` 크기 확인: 약 `319M`.

검증:
- 정리 전 `npm run build` 성공.
- 정리 후 `npm run build` 성공.
- 이동 때문에 깨진 import는 확인되지 않았다.

빌드 경고:
- `services/labPromptBuilder.ts`가 `components/ShortsLabPanel.tsx` 등에서 정적 import와 동적 import에 동시에 걸려 있다.
- 이 때문에 동적 import를 해도 별도 chunk로 분리되지 않는다는 Vite 경고가 나온다.
- 최종 번들 크기: `dist/assets/index-CzKQPEGm.js` 약 `2.28MB`, gzip 약 `629.70KB`.
- 향후 번들 최적화를 하려면 `labPromptBuilder.ts` import 방식을 정리하고, 쇼츠랩/마스터스튜디오/AI스튜디오 단위로 code splitting을 다시 설계하는 것이 좋다.

중요 판단:
- `ai_studio_bundle/`은 루트 보조 폴더처럼 보이지만 `components/AiStudioHost.tsx`에서 `../ai_studio_bundle/App`으로 실제 import 중이다.
- 따라서 현재 앱 실행 의존성이 있으므로 이동하지 않았다.
- `puppeteer_user_data/`는 `server/puppeteerHandler.js`에서 실제 사용한다.
- 따라서 브라우저 세션/자동화 프로필 보존을 위해 이동하지 않았다.

추가 정리 후보:
- `standalone-lite/`
- `pdp-maker/`
- `pdp-maker-201-main/`
- `.tmp.driveupload/`
- `user_data/`
- `user_data_debug/`
- `user_data_fresh/`
- `user_data_image/`
- `user_data_script/`
- `user_data_stealth/`
- `이미지/`
- `샘플이미지/`

주의:
- `standalone-lite/`, `pdp-maker/`, `pdp-maker-201-main/`은 현재 앱 빌드 경로에서 직접 참조되지 않는 별도/보조 프로젝트로 보인다.
- 다만 마마님 작업물일 가능성이 있어 자동 이동하지 않았다.
- `user_data_*` 계열과 `.tmp.driveupload/`는 용량이 크지만 자동화 세션, 업로드, 이미지 작업 데이터일 수 있어 삭제하지 않았다.

추천 다음 단계:
- 별도 프로젝트 보관 폴더를 만든다: `archive/side-projects/`
- `standalone-lite/`, `pdp-maker/`, `pdp-maker-201-main/`를 해당 폴더로 이동할지 결정한다.
- `user_data_*`는 최근 사용 여부를 확인한 뒤 오래된 브라우저 프로필만 압축 또는 삭제한다.
- `.tmp.driveupload/`는 현재 업로드 작업이 없다면 보관 또는 삭제 후보로 분류한다.
- `.gitignore`에 루트 임시 산출물 패턴을 추가해 같은 문제가 반복되지 않게 한다.

반복 방지용 `.gitignore` 추천 패턴:
- `*.bak`
- `*.backup*`
- `*.tmp`
- `temp_*`
- `debug_*`
- `grok_*`
- `demo_*`
- `test_output*`
- `*_backup_*`
- `root_cleanup_*`
- `docs/root_archive_*`
