# 쇼츠랩 의존성 안전 개선 작업계획

작성일: 2026-06-13

## 1. 목적

쇼츠랩은 현재 기능이 많고 버튼 로직이 복잡해서 작은 수정도 시간이 오래 걸린다.  
이 문서는 기존 기능을 최대한 깨지 않으면서 다음 문제를 단계적으로 해결하기 위한 작업계획이다.

- `ShortsLabPanel.tsx` 비대화
- 일반 흐름, 수동 흐름, V4 흐름의 프롬프트 생성 로직 분산
- 캐릭터, 3면도, 의상, 슬롯 규칙의 동기화 불투명
- AI 응답 JSON 파싱 실패 원인 추적 어려움
- 버튼 이름과 실제 동작 불일치
- 기능 추가 시 기존 버튼에 영향이 퍼지는 문제

핵심 원칙은 “기존 로직을 바로 갈아엎지 않고, 공통 래퍼와 검사기를 먼저 붙인 뒤 점진적으로 옮기는 것”이다.

## 2. 현재 가장 위험한 구조

### 2.1 `ShortsLabPanel.tsx`가 너무 많은 책임을 갖고 있음

현재 이 파일에는 다음 책임이 함께 들어 있다.

- 화면 UI
- 버튼 클릭 핸들러
- 줄거리 생성 상태
- 일반 대본 생성 상태
- V4 생성 상태
- 수동 장면분해
- 캐릭터 규칙 관리
- 3면도 연결
- 씬 후처리
- 최종 이미지 프롬프트 보정
- 토스트/로딩/에러 처리

문제:
- 버튼 하나를 수정해도 다른 흐름에 영향이 있는지 확인하기 어렵다.
- 로직과 UI가 섞여 있어 테스트하기 어렵다.
- 일반/V4/수동 흐름의 중복 로직이 서로 다르게 진화할 가능성이 크다.

### 2.2 최종 이미지 프롬프트 생성 경로가 여러 개임

현재 주요 경로:

- 일반 마스터 생성
- 줄거리 선택 후 대본 생성
- 수동 장면분해
- V4 줄거리/대본/씬 생성
- 캐릭터 정보 적용
- 3면도 연결
- 후처리 적용

문제:
- 어떤 경로는 캐릭터가 잘 들어가고, 어떤 경로는 의상이나 3면도가 빠질 수 있다.
- Safe Glamour, cleanup, prefix/suffix 적용 순서가 흐름별로 달라질 수 있다.
- 최종적으로 이미지 생성 직전에 어떤 prompt가 쓰였는지 추적하기 어렵다.

### 2.3 캐릭터 관련 데이터 계층이 많음

관련 데이터:

- 저장 캐릭터
- 쇼츠랩 캐릭터 슬롯 규칙
- `masterCharacterProfiles`
- scene의 `characterIds`
- V4 slot
- `lockedOutfits`
- `outfitMap`
- 3면도/참조 이미지
- 최종 prompt 내부 Person 블록

문제:
- 저장 캐릭터에는 있는데 슬롯 규칙에는 없을 수 있다.
- scene에는 캐릭터 ID가 있는데 실제 캐릭터/3면도/의상이 없을 수 있다.
- 최종 prompt에는 다른 캐릭터가 들어갔는데 UI에서는 알기 어렵다.

## 3. 의존성 충돌을 피하는 작업 원칙

### 3.1 기존 버튼의 직접 수정은 마지막에 한다

처음부터 버튼 핸들러를 크게 바꾸지 않는다.  
먼저 현재 핸들러 바깥에 검사기, 어댑터, 공통 파이프라인을 추가한다.

권장 순서:

1. 현재 입력/출력 구조를 그대로 관찰한다.
2. 공통 함수는 기존 결과와 동일한 결과를 내도록 만든다.
3. 기존 함수와 새 함수를 병렬 비교한다.
4. 결과 차이가 작고 의도된 경우에만 버튼 연결을 바꾼다.

### 3.2 새 로직은 먼저 “읽기 전용 검사기”로 만든다

처음부터 scene이나 prompt를 수정하지 않는다.  
먼저 문제를 찾기만 하는 검사기를 만든다.

예:

- 캐릭터 ID 누락 검사
- 3면도 연결 여부 검사
- 의상 누락 검사
- Person 블록 개수 검사
- `shotType`과 `characterIds` 불일치 검사
- 최종 prompt 비어 있음 검사

이렇게 하면 기존 기능을 깨지 않고도 어디가 문제인지 볼 수 있다.

### 3.3 공통 파이프라인은 feature flag로 연결한다

새 공통 프롬프트 파이프라인을 만들더라도 바로 전체 적용하지 않는다.

권장 방식:

```text
useUnifiedPromptPipeline: false
```

초기에는 기본값을 `false`로 둔다.  
UI에서 개발용 토글 또는 내부 설정으로만 켠다.

효과:
- 기존 사용자는 기존 방식 그대로 사용한다.
- 새 로직은 특정 테스트에서만 켜서 비교할 수 있다.
- 문제가 생기면 바로 기존 경로로 돌아갈 수 있다.

### 3.4 파일 분리는 “복사 후 연결”이 아니라 “얇은 래퍼부터” 시작한다

`ShortsLabPanel.tsx`에서 코드를 대량 이동하면 충돌이 크다.  
처음에는 작은 헬퍼부터 빼는 방식이 안전하다.

안전한 분리 순서:

1. 순수 함수 분리
2. 타입 분리
3. 검사기 분리
4. 후처리 함수 분리
5. 훅 분리
6. UI 컴포넌트 분리

가장 위험한 분리:

- 상태 전체를 한 번에 훅으로 이동
- 버튼 핸들러 전체를 한 번에 이동
- V4 흐름과 일반 흐름을 동시에 변경

## 4. 추천 목표 구조

### 4.1 쇼츠랩 파일 구조 제안

```text
components/
  ShortsLabPanel.tsx
  shorts-lab/
    StorylineControls.tsx
    SceneList.tsx
    CharacterConnectionPanel.tsx
    PromptDebugPanel.tsx
    GenerationTimeline.tsx

hooks/
  useShortsLabStorylineFlow.ts
  useShortsLabV4Flow.ts
  useShortsLabCharacterConnections.ts
  useShortsLabScenePostProcess.ts

services/
  shorts-lab/
    finalPromptPipeline.ts
    sceneDiagnostics.ts
    characterConnectionDiagnostics.ts
    promptTrace.ts
```

주의:
- 처음부터 이 구조를 모두 만들 필요는 없다.
- 첫 단계에서는 `services/shorts-lab/sceneDiagnostics.ts`와 `finalPromptPipeline.ts`만 만들어도 충분하다.

### 4.2 최종 프롬프트 공통 파이프라인 목표

최종 목표는 모든 흐름이 아래 단계를 공유하는 것이다.

```text
raw scene
-> normalize scene
-> resolve character IDs
-> resolve character profiles
-> resolve 3면도 reference
-> resolve outfit
-> resolve shotType
-> resolve cameraAngle
-> build base prompt
-> validate prompt
-> apply Safe Glamour
-> apply cleanup patterns
-> apply prefix/suffix
-> final prompt
```

출력 구조 예:

```ts
interface FinalPromptResult {
  sceneNumber: number;
  prompt: string;
  characterIds: string[];
  shotType: '원샷' | '투샷' | '쓰리샷';
  cameraAngle: string;
  warnings: string[];
  trace: PromptTraceStep[];
}
```

중요:
- 처음부터 기존 prompt를 바꾸지 말고 `trace`와 `warnings`만 생성한다.
- 기존 prompt와 새 prompt를 비교하는 기능부터 만든다.

## 5. 단계별 작업계획

## Phase 0. 기준선 고정

목표:
- 변경 전 상태를 정확히 저장하고, 문제 발생 시 돌아갈 수 있게 한다.

작업:
- `git status` 확인
- `npm run build` 성공 확인
- 현재 쇼츠랩 생성 결과 샘플 1개 저장
- V4 생성 결과 샘플 1개 저장
- 수동 장면분해 결과 샘플 1개 저장

산출물:
- 기준 샘플 JSON
- 기준 prompt 예시
- 기준 빌드 결과

주의:
- 이 단계에서는 코드 변경을 하지 않는다.

## Phase 1. 읽기 전용 진단기 추가

목표:
- 기존 로직을 건드리지 않고 문제를 눈으로 볼 수 있게 한다.

추가할 파일:

```text
services/shorts-lab/sceneDiagnostics.ts
```

검사 항목:

- scene number 누락
- text/scriptLine 누락
- prompt 누락
- `characterIds` 없음
- `characterIds`가 슬롯 규칙에 없음
- 저장 캐릭터 없음
- 3면도 참조 없음
- 의상 없음
- `shotType`과 캐릭터 수 불일치
- prompt 내부 Person 블록 수 불일치
- prompt에 카메라 앵글 없음
- prompt에 배경 없음

출력 예:

```ts
interface SceneDiagnosticIssue {
  severity: 'error' | 'warning' | 'info';
  sceneNumber?: number;
  code: string;
  message: string;
}
```

UI 연결:
- 처음에는 콘솔 로그 또는 개발용 접기 패널로만 보여준다.
- 기존 생성 결과를 수정하지 않는다.

검증:
- 기존 생성 버튼 동작이 바뀌지 않아야 한다.
- `npm run build` 성공.

## Phase 2. Prompt Trace 추가

목표:
- 최종 prompt가 어떻게 만들어졌는지 단계별로 확인할 수 있게 한다.

추가할 파일:

```text
services/shorts-lab/promptTrace.ts
```

Trace 단계:

- AI 원본 scene
- 캐릭터 적용 전
- 캐릭터 적용 후
- 의상 적용 후
- 카메라/shotType 보정 후
- 후처리 전
- 후처리 후
- 이미지 생성 직전

출력 예:

```ts
interface PromptTraceStep {
  name: string;
  prompt: string;
  changed: boolean;
  notes?: string[];
}
```

UI:
- 씬 상세 모달에 `프롬프트 추적` 탭 추가.
- 처음에는 읽기 전용으로만 표시.

검증:
- 기존 prompt 출력과 이미지 생성 결과가 바뀌지 않아야 한다.

## Phase 3. 최종 프롬프트 공통 파이프라인을 “비활성 상태”로 추가

목표:
- 새 파이프라인을 만들지만 기본 동작에는 연결하지 않는다.

추가할 파일:

```text
services/shorts-lab/finalPromptPipeline.ts
```

처음 구현 범위:

- 입력 scene을 받는다.
- 기존 scene.prompt를 그대로 유지한다.
- diagnostics와 trace만 만든다.
- `finalPrompt`는 기존 prompt와 동일하게 반환한다.

초기 형태:

```ts
export function buildFinalPromptPreview(input: FinalPromptInput): FinalPromptResult {
  return {
    sceneNumber: input.scene.number,
    prompt: input.scene.prompt || '',
    characterIds: input.scene.characterIds || [],
    shotType: resolvedShotType,
    cameraAngle: resolvedCameraAngle,
    warnings,
    trace
  };
}
```

중요:
- 이 단계에서는 prompt 재작성 금지.
- 기존 결과와 새 결과가 동일해야 한다.

검증:
- 일반 생성, V4 생성, 수동 장면분해 각각 1회씩 실행.
- `finalPromptPipeline` 결과가 기존 prompt와 같은지 확인.

## Phase 4. 캐릭터/3면도 연결 검사기 추가

목표:
- `3면도` 버튼이 실제로 어떤 캐릭터에 연결됐는지 바로 확인한다.

추가할 파일:

```text
services/shorts-lab/characterConnectionDiagnostics.ts
```

검사 항목:

- scene에 등장한 캐릭터 ID
- 슬롯 규칙 존재 여부
- 저장 캐릭터 존재 여부
- 3면도 참조 존재 여부
- 의상 존재 여부
- prompt 내부 반영 여부

UI 표시 예:

```text
Scene 3
- WomanA: 저장 캐릭터 있음 / 3면도 있음 / 의상 있음 / prompt 반영됨
- ManA: 저장 캐릭터 있음 / 3면도 없음 / 의상 있음 / prompt 반영됨
```

효과:
- 3면도 버튼을 눌렀는데 실제로 안 붙는 문제를 바로 볼 수 있다.
- 캐릭터가 프롬프트에 빠지는 문제를 빠르게 찾을 수 있다.

## Phase 5. 버튼 이름과 버튼 조건 정리

목표:
- 사용자가 버튼을 눌렀을 때 기대하는 결과와 실제 결과를 맞춘다.

수정 후보:

- `AI 줄거리 생성`
  - 현재 주제 또는 벤치마크 원문 기반 생성 가능해야 한다.
  - 버튼 disabled 조건이 `aiTopic`만 보는지 확인 필요.

- `이 줄거리로 대본 생성`
  - 실제로 대본만 생성한다면 `이 줄거리로 대본만 생성`으로 변경.

- `AI 마스터 생성`
  - 구조/씬/prompt까지 만드는지 정확히 표시.
  - 필요하면 `AI 마스터 구조 생성`으로 변경.

- `V4 줄거리`
  - V4 전체 생성이 아니라 줄거리만 생성한다면 이름 유지.
  - 줄거리 선택 후 V4 대본/씬 생성 버튼과 명확히 분리.

주의:
- 버튼 이름 변경은 기능 변경보다 안전하지만, 사용자 혼란을 줄이는 효과가 크다.

## Phase 6. 공통 파이프라인을 일부 흐름에만 적용

목표:
- 모든 흐름에 한 번에 적용하지 않는다.
- 가장 위험이 낮은 흐름부터 새 파이프라인을 실제 적용한다.

추천 적용 순서:

1. 수동 장면분해 결과
2. 일반 마스터 생성 결과
3. 줄거리 선택 후 대본 생성 이후 장면분해
4. V4 생성 결과

이유:
- V4는 이미 별도 서비스가 있고 최근 변경이 많아 충돌 위험이 크다.
- 수동 장면분해는 비교적 입력과 출력이 명확해서 테스트하기 쉽다.

검증 방식:

```text
기존 prompt
새 파이프라인 prompt
차이점
문제 여부
```

차이가 생겨도 바로 덮어쓰지 말고 diff UI에서 먼저 확인한다.

## Phase 7. 실패 복구 버튼 추가

목표:
- 전체 재생성이 아니라 실패한 단계만 다시 실행한다.

추가 버튼:

- `현재 대본으로 씬 분해 재시도`
- `캐릭터/3면도 다시 적용`
- `의상만 다시 배정`
- `프롬프트만 다시 생성`
- `이 씬만 이미지 재생성`

의존성 주의:
- 실패 복구 버튼은 기존 생성 버튼과 별도 핸들러로 만든다.
- 기존 버튼 핸들러 내부에 조건문을 계속 추가하지 않는다.

## Phase 8. JSON 파싱 디버그 패널 추가

목표:
- AI 응답 파싱 실패 원인을 바로 확인한다.

표시할 정보:

- LLM 원문 응답
- JSON 추출 전 텍스트
- JSON 추출 결과
- `jsonrepair` 적용 결과
- normalize 후 scenes
- 실패 에러 메시지

주의:
- API 키나 민감정보가 들어가지 않게 필터링한다.
- 기본은 접힌 상태로 둔다.

## 6. 작업 중 절대 피해야 할 방식

### 6.1 `ShortsLabPanel.tsx`를 한 번에 대규모 분리

위험:
- 충돌이 많다.
- 상태 참조가 깨질 가능성이 높다.
- 버튼별 동작 차이를 놓치기 쉽다.

대안:
- 순수 함수부터 분리.
- 진단기부터 추가.
- 훅 분리는 마지막에 한다.

### 6.2 일반/V4/수동 흐름을 동시에 수정

위험:
- 어떤 변경이 어떤 버그를 만들었는지 추적하기 어렵다.

대안:
- 한 번에 한 흐름만 적용.
- 나머지 흐름은 읽기 전용 진단만 붙인다.

### 6.3 기존 prompt를 즉시 새 prompt로 덮어쓰기

위험:
- 이미지 품질이 갑자기 변한다.
- 기존 사용자가 의도한 스타일이 깨질 수 있다.

대안:
- 기존 prompt와 새 prompt를 먼저 diff로 비교.
- 특정 흐름에서만 feature flag로 켠다.

### 6.4 백업 파일을 계속 코드 폴더에 추가

위험:
- `*.bak`, `debug_bak`, `backup_날짜` 파일이 실제 코드처럼 보인다.
- 검색 결과가 오염된다.
- 다음 수정자가 잘못된 파일을 볼 수 있다.

대안:
- 백업은 `backups/` 아래로만 이동.
- 실제 코드 폴더에는 실행 파일만 둔다.
- `.gitignore` 패턴을 강화한다.

## 7. 우선순위 결론

가장 먼저 할 일:

```text
1. sceneDiagnostics.ts 추가
2. promptTrace.ts 추가
3. finalPromptPipeline.ts를 비활성 preview 모드로 추가
```

이 3개는 기존 버튼 동작을 거의 건드리지 않으면서 문제를 눈으로 보이게 만든다.

그 다음:

```text
4. 캐릭터/3면도 연결 검사기 추가
5. 버튼 이름/disabled 조건 정리
6. 수동 장면분해부터 공통 파이프라인 실제 적용
```

마지막:

```text
7. V4에 공통 파이프라인 적용
8. ShortsLabPanel.tsx 훅/컴포넌트 분리
9. 실패 복구 버튼과 JSON 파싱 디버그 패널 추가
```

## 8. 추천 첫 작업 단위

첫 PR 또는 첫 커밋은 작게 잡는 것이 좋다.

추천 첫 커밋:

```text
feat: add shorts lab scene diagnostics
```

포함할 것:

- `services/shorts-lab/sceneDiagnostics.ts`
- diagnostics 타입
- scene 배열을 받아 issue 배열을 반환하는 순수 함수
- `ShortsLabPanel.tsx`에는 최소 연결만 추가
- UI는 console 또는 접힌 개발용 패널 정도만 추가

포함하지 말 것:

- prompt 생성 로직 변경
- 버튼 핸들러 구조 변경
- V4 흐름 변경
- 캐릭터 저장 로직 변경

검증:

- `npm run build`
- 일반 줄거리 생성 UI 진입 확인
- V4 줄거리 UI 진입 확인
- 기존 이미지 프롬프트 값이 바뀌지 않았는지 확인

## 9. 체크리스트

작업 전:

- [ ] `git status` 확인
- [ ] 최신 `origin/master` fetch
- [ ] `npm run build` 기준선 확인
- [ ] 변경 대상 파일 목록 확정

작업 중:

- [ ] 한 번에 하나의 흐름만 수정
- [ ] 새 로직은 먼저 읽기 전용으로 추가
- [ ] 기존 prompt 값을 바로 덮어쓰지 않기
- [ ] 백업 파일을 코드 폴더에 만들지 않기

작업 후:

- [ ] `npm run build`
- [ ] 변경 파일 수 확인
- [ ] `ShortsLabPanel.tsx` 변경량 확인
- [ ] 문서 업데이트
- [ ] 커밋 메시지 작성

## 10. 최종 목표

최종적으로는 마마님이 버튼 하나를 수정할 때 다음처럼 바로 확인할 수 있어야 한다.

```text
이 버튼은 어떤 입력을 받는가?
어떤 서비스 함수를 호출하는가?
AI 응답은 어디서 파싱되는가?
scene은 어디서 만들어지는가?
캐릭터/3면도/의상은 어디서 붙는가?
최종 이미지 prompt는 어디서 완성되는가?
실패하면 어느 단계만 재시도하면 되는가?
```

이 구조가 되면 쇼츠랩은 기능 추가가 쉬워지고, 기존 버튼을 고칠 때도 영향 범위를 좁혀서 작업할 수 있다.
